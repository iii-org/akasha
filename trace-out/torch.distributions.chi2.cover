       # mypy: allow-untyped-defs
    1: from typing import Optional, Union
       
    1: from torch import Tensor
    1: from torch.distributions import constraints
    1: from torch.distributions.gamma import Gamma
       
       
    1: __all__ = ["Chi2"]
       
       
    2: class Chi2(Gamma):
    1:     r"""
           Creates a Chi-squared distribution parameterized by shape parameter :attr:`df`.
           This is exactly equivalent to ``Gamma(alpha=0.5*df, beta=0.5)``
       
           Example::
       
               >>> # xdoctest: +IGNORE_WANT("non-deterministic")
               >>> m = Chi2(torch.tensor([1.0]))
               >>> m.sample()  # Chi2 distributed with shape df=1
               tensor([ 0.1046])
       
           Args:
               df (float or Tensor): shape parameter of the distribution
           """
       
    1:     arg_constraints = {"df": constraints.positive}
       
    4:     def __init__(
               self,
    1:         df: Union[Tensor, float],
    2:         validate_args: Optional[bool] = None,
    1:     ) -> None:
               super().__init__(0.5 * df, 0.5, validate_args=validate_args)
       
    1:     def expand(self, batch_shape, _instance=None):
               new = self._get_checked_instance(Chi2, _instance)
               return super().expand(batch_shape, new)
       
    2:     @property
    2:     def df(self) -> Tensor:
               return self.concentration * 2
