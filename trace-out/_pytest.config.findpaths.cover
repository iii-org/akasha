    1: from __future__ import annotations
       
    1: from collections.abc import Iterable
    1: from collections.abc import Sequence
    1: from dataclasses import dataclass
    1: from dataclasses import KW_ONLY
    1: import os
    1: from pathlib import Path
    1: import sys
    1: from typing import Literal
    1: from typing import TypeAlias
       
    1: import iniconfig
       
    1: from .exceptions import UsageError
    1: from _pytest.outcomes import fail
    1: from _pytest.pathlib import absolutepath
    1: from _pytest.pathlib import commonpath
    1: from _pytest.pathlib import safe_exists
       
       
    3: @dataclass(frozen=True)
    2: class ConfigValue:
    1:     """Represents a configuration value with its origin and parsing mode.
       
           This allows tracking whether a value came from a configuration file
           or from a CLI override (--override-ini), which is important for
           determining precedence when dealing with ini option aliases.
       
           The mode tracks the parsing mode/data model used for the value:
           - "ini": from INI files or [tool.pytest.ini_options], where the only
             supported value types are `str` or `list[str]`.
           - "toml": from TOML files (not in INI mode), where native TOML types
              are preserved.
           """
       
    1:     value: object
    1:     _: KW_ONLY
    1:     origin: Literal["file", "override"]
    1:     mode: Literal["ini", "toml"]
       
       
    1: ConfigDict: TypeAlias = dict[str, ConfigValue]
       
       
    1: def _parse_ini_config(path: Path) -> iniconfig.IniConfig:
           """Parse the given generic '.ini' file using legacy IniConfig parser, returning
           the parsed object.
       
           Raise UsageError if the file cannot be parsed.
           """
    1:     try:
    1:         return iniconfig.IniConfig(str(path))
           except iniconfig.ParseError as exc:
               raise UsageError(str(exc)) from exc
       
       
    1: def load_config_dict_from_file(
           filepath: Path,
       ) -> ConfigDict | None:
           """Load pytest configuration from the given file path, if supported.
       
           Return None if the file does not contain valid pytest configuration.
           """
           # Configuration from ini files are obtained from the [pytest] section, if present.
    2:     if filepath.suffix == ".ini":
    1:         iniconfig = _parse_ini_config(filepath)
       
    1:         if "pytest" in iniconfig:
   11:             return {
    4:                 k: ConfigValue(v, origin="file", mode="ini")
    5:                 for k, v in iniconfig["pytest"].items()
                   }
               else:
                   # "pytest.ini" files are always the source of configuration, even if empty.
                   if filepath.name in {"pytest.ini", ".pytest.ini"}:
                       return {}
       
           # '.cfg' files are considered if they contain a "[tool:pytest]" section.
    1:     elif filepath.suffix == ".cfg":
               iniconfig = _parse_ini_config(filepath)
       
               if "tool:pytest" in iniconfig.sections:
                   return {
                       k: ConfigValue(v, origin="file", mode="ini")
                       for k, v in iniconfig["tool:pytest"].items()
                   }
               elif "pytest" in iniconfig.sections:
                   # If a setup.cfg contains a "[pytest]" section, we raise a failure to indicate users that
                   # plain "[pytest]" sections in setup.cfg files is no longer supported (#3086).
                   fail(CFG_PYTEST_SECTION.format(filename="setup.cfg"), pytrace=False)
       
           # '.toml' files are considered if they contain a [tool.pytest] table (toml mode)
           # or [tool.pytest.ini_options] table (ini mode) for pyproject.toml,
           # or [pytest] table (toml mode) for pytest.toml/.pytest.toml.
    1:     elif filepath.suffix == ".toml":
    1:         if sys.version_info >= (3, 11):
    1:             import tomllib
               else:
                   import tomli as tomllib
       
    1:         toml_text = filepath.read_text(encoding="utf-8")
    1:         try:
    1:             config = tomllib.loads(toml_text)
               except tomllib.TOMLDecodeError as exc:
                   raise UsageError(f"{filepath}: {exc}") from exc
       
               # pytest.toml and .pytest.toml use [pytest] table directly.
    1:         if filepath.name in ("pytest.toml", ".pytest.toml"):
                   pytest_config = config.get("pytest", {})
                   if pytest_config:
                       # TOML mode - preserve native TOML types.
                       return {
                           k: ConfigValue(v, origin="file", mode="toml")
                           for k, v in pytest_config.items()
                       }
                   # "pytest.toml" files are always the source of configuration, even if empty.
                   return {}
       
               # pyproject.toml uses [tool.pytest] or [tool.pytest.ini_options].
               else:
    1:             tool_pytest = config.get("tool", {}).get("pytest", {})
       
                   # Check for toml mode config: [tool.pytest] with content outside of ini_options.
    3:             toml_config = {k: v for k, v in tool_pytest.items() if k != "ini_options"}
                   # Check for ini mode config: [tool.pytest.ini_options].
    1:             ini_config = tool_pytest.get("ini_options", None)
       
    1:             if toml_config and ini_config:
                       raise UsageError(
                           f"{filepath}: Cannot use both [tool.pytest] (native TOML types) and "
                           "[tool.pytest.ini_options] (string-based INI format) simultaneously. "
                           "Please use [tool.pytest] with native TOML types (recommended) "
                           "or [tool.pytest.ini_options] for backwards compatibility."
                       )
       
    1:             if toml_config:
                       # TOML mode - preserve native TOML types.
                       return {
                           k: ConfigValue(v, origin="file", mode="toml")
                           for k, v in toml_config.items()
                       }
       
    1:             elif ini_config is not None:
                       # INI mode - TOML supports richer data types than INI files, but we need to
                       # convert all scalar values to str for compatibility with the INI system.
    1:                 def make_scalar(v: object) -> str | list[str]:
    2:                     return v if isinstance(v, list) else str(v)
       
    7:                 return {
    2:                     k: ConfigValue(make_scalar(v), origin="file", mode="ini")
    3:                     for k, v in ini_config.items()
                       }
       
           return None
       
       
    1: def locate_config(
           invocation_dir: Path,
           args: Iterable[Path],
       ) -> tuple[Path | None, Path | None, ConfigDict, Sequence[str]]:
           """Search in the list of arguments for a valid ini-file for pytest,
           and return a tuple of (rootdir, inifile, cfg-dict, ignored-config-files), where
           ignored-config-files is a list of config basenames found that contain
           pytest configuration but were ignored."""
    1:     config_names = [
               "pytest.toml",
               ".pytest.toml",
               "pytest.ini",
               ".pytest.ini",
               "pyproject.toml",
               "tox.ini",
               "setup.cfg",
           ]
    3:     args = [x for x in args if not str(x).startswith("-")]
    1:     if not args:
               args = [invocation_dir]
    1:     found_pyproject_toml: Path | None = None
    1:     ignored_config_files: list[str] = []
       
    1:     for arg in args:
    1:         argpath = absolutepath(arg)
    3:         for base in (argpath, *argpath.parents):
   19:             for config_name in config_names:
   17:                 p = base / config_name
   17:                 if p.is_file():
    1:                     if p.name == "pyproject.toml" and found_pyproject_toml is None:
                               found_pyproject_toml = p
    1:                     ini_config = load_config_dict_from_file(p)
    1:                     if ini_config is not None:
    1:                         index = config_names.index(config_name)
    5:                         for remainder in config_names[index + 1 :]:
    4:                             p2 = base / remainder
    4:                             if (
    4:                                 p2.is_file()
    1:                                 and load_config_dict_from_file(p2) is not None
                                   ):
    1:                                 ignored_config_files.append(remainder)
    1:                         return base, p, ini_config, ignored_config_files
           if found_pyproject_toml is not None:
               return found_pyproject_toml.parent, found_pyproject_toml, {}, []
           return None, None, {}, []
       
       
    1: def get_common_ancestor(
           invocation_dir: Path,
           paths: Iterable[Path],
       ) -> Path:
    1:     common_ancestor: Path | None = None
    2:     for path in paths:
    1:         if not path.exists():
                   continue
    1:         if common_ancestor is None:
    1:             common_ancestor = path
               else:
                   if common_ancestor in path.parents or path == common_ancestor:
                       continue
                   elif path in common_ancestor.parents:
                       common_ancestor = path
                   else:
                       shared = commonpath(path, common_ancestor)
                       if shared is not None:
                           common_ancestor = shared
    1:     if common_ancestor is None:
               common_ancestor = invocation_dir
    1:     elif common_ancestor.is_file():
               common_ancestor = common_ancestor.parent
    1:     return common_ancestor
       
       
    1: def get_dirs_from_args(args: Iterable[str]) -> list[Path]:
    1:     def is_option(x: str) -> bool:
    1:         return x.startswith("-")
       
    1:     def get_file_part_from_node_id(x: str) -> str:
    1:         return x.split("::", maxsplit=1)[0]
       
    1:     def get_dir_from_path(path: Path) -> Path:
    1:         if path.is_dir():
                   return path
    1:         return path.parent
       
           # These look like paths but may not exist
    6:     possible_paths = (
    1:         absolutepath(get_file_part_from_node_id(arg))
    2:         for arg in args
    1:         if not is_option(arg)
           )
       
    3:     return [get_dir_from_path(path) for path in possible_paths if safe_exists(path)]
       
       
    1: def parse_override_ini(override_ini: Sequence[str] | None) -> ConfigDict:
           """Parse the -o/--override-ini command line arguments and return the overrides.
       
           :raises UsageError:
               If one of the values is malformed.
           """
    2:     overrides = {}
           # override_ini is a list of "ini=value" options.
           # Always use the last item if multiple values are set for same ini-name,
           # e.g. -o foo=bar1 -o foo=bar2 will set foo to bar2.
    2:     for ini_config in override_ini or ():
               try:
                   key, user_ini_value = ini_config.split("=", 1)
               except ValueError as e:
                   raise UsageError(
                       f"-o/--override-ini expects option=value style (got: {ini_config!r})."
                   ) from e
               else:
                   overrides[key] = ConfigValue(user_ini_value, origin="override", mode="ini")
    2:     return overrides
       
       
    1: CFG_PYTEST_SECTION = "[pytest] section in {filename} files is no longer supported, change to [tool:pytest] instead."
       
       
    1: def determine_setup(
           *,
           inifile: str | None,
           override_ini: Sequence[str] | None,
           args: Sequence[str],
           rootdir_cmd_arg: str | None,
           invocation_dir: Path,
       ) -> tuple[Path, Path | None, ConfigDict, Sequence[str]]:
           """Determine the rootdir, inifile and ini configuration values from the
           command line arguments.
       
           :param inifile:
               The `--inifile` command line argument, if given.
           :param override_ini:
               The -o/--override-ini command line arguments, if given.
           :param args:
               The free command line arguments.
           :param rootdir_cmd_arg:
               The `--rootdir` command line argument, if given.
           :param invocation_dir:
               The working directory when pytest was invoked.
       
           :raises UsageError:
           """
    1:     rootdir = None
    1:     dirs = get_dirs_from_args(args)
    1:     ignored_config_files: Sequence[str] = []
       
    1:     if inifile:
               inipath_ = absolutepath(inifile)
               inipath: Path | None = inipath_
               inicfg = load_config_dict_from_file(inipath_) or {}
               if rootdir_cmd_arg is None:
                   rootdir = inipath_.parent
           else:
    1:         ancestor = get_common_ancestor(invocation_dir, dirs)
    2:         rootdir, inipath, inicfg, ignored_config_files = locate_config(
    1:             invocation_dir, [ancestor]
               )
    1:         if rootdir is None and rootdir_cmd_arg is None:
                   for possible_rootdir in (ancestor, *ancestor.parents):
                       if (possible_rootdir / "setup.py").is_file():
                           rootdir = possible_rootdir
                           break
                   else:
                       if dirs != [ancestor]:
                           rootdir, inipath, inicfg, _ = locate_config(invocation_dir, dirs)
                       if rootdir is None:
                           rootdir = get_common_ancestor(
                               invocation_dir, [invocation_dir, ancestor]
                           )
                           if is_fs_root(rootdir):
                               rootdir = ancestor
    1:     if rootdir_cmd_arg:
               rootdir = absolutepath(os.path.expandvars(rootdir_cmd_arg))
               if not rootdir.is_dir():
                   raise UsageError(
                       f"Directory '{rootdir}' not found. Check your '--rootdir' option."
                   )
       
    1:     ini_overrides = parse_override_ini(override_ini)
    1:     inicfg.update(ini_overrides)
       
    1:     assert rootdir is not None
    1:     return rootdir, inipath, inicfg, ignored_config_files
       
       
    1: def is_fs_root(p: Path) -> bool:
           r"""
           Return True if the given path is pointing to the root of the
           file system ("/" on Unix and "C:\\" on Windows for example).
           """
           return os.path.splitdrive(str(p))[1] == os.sep
