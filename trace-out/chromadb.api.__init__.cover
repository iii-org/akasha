    1: from chromadb.api.types import *  # noqa: F401, F403
    1: from chromadb.execution.expression import (  # noqa: F401, F403
           Search,
           Key,
           K,
           SearchWhere,
           And,
           Or,
           Eq,
           Ne,
           Gt,
           Gte,
           Lt,
           Lte,
           In,
           Nin,
           Regex,
           NotRegex,
           Contains,
           NotContains,
           Limit,
           Select,
           Rank,
           Abs,
           Div,
           Exp,
           Log,
           Max,
           Min,
           Mul,
           Knn,
           Rrf,
           Sub,
           Sum,
           Val,
           Aggregate,
           MinK,
           MaxK,
           GroupBy,
       )
       
    1: from abc import ABC, abstractmethod
    1: from typing import Sequence, Optional, List, Dict, Any, Tuple
    1: from uuid import UUID
       
    1: from overrides import override
    1: from chromadb.api.collection_configuration import (
           CreateCollectionConfiguration,
           UpdateCollectionConfiguration,
       )
    1: from chromadb.config import DEFAULT_DATABASE, DEFAULT_TENANT
    1: from chromadb.api.types import (
           CollectionMetadata,
           DeleteResult,
           Documents,
           Embeddable,
           EmbeddingFunction,
           DataLoader,
           Embeddings,
           IDs,
           Include,
           IncludeMetadataDocumentsDistances,
           IncludeMetadataDocuments,
           Loadable,
           Metadatas,
           ReadLevel,
           Schema,
           URIs,
           Where,
           QueryResult,
           GetResult,
           WhereDocument,
           SearchResult,
           DefaultEmbeddingFunction,
       )
       
    1: from chromadb.auth import UserIdentity
    1: from chromadb.config import Component, Settings
    1: from chromadb.types import Database, Tenant, Collection as CollectionModel
    1: from chromadb.api.models.Collection import Collection
    1: from chromadb.api.models.AttachedFunction import AttachedFunction
       
       # Re-export the async version
    1: from chromadb.api.async_api import (  # noqa: F401
           AsyncBaseAPI as AsyncBaseAPI,
           AsyncClientAPI as AsyncClientAPI,
           AsyncAdminAPI as AsyncAdminAPI,
           AsyncServerAPI as AsyncServerAPI,
       )
       
       
    2: class BaseAPI(ABC):
    2:     @abstractmethod
    2:     def heartbeat(self) -> int:
               """Get the current time in nanoseconds since epoch.
               Used to check if the server is alive.
       
               Returns:
                   int: The current time in nanoseconds since epoch
       
               """
               pass
       
           #
           # COLLECTION METHODS
           #
    2:     @abstractmethod
    2:     def count_collections(self) -> int:
               """Count the number of collections.
       
               Returns:
                   int: The number of collections.
       
               Examples:
                   ```python
                   client.count_collections()
                   # 1
                   ```
               """
               pass
       
    6:     def _modify(
               self,
    1:         id: UUID,
    2:         new_name: Optional[str] = None,
    2:         new_metadata: Optional[CollectionMetadata] = None,
    2:         new_configuration: Optional[UpdateCollectionConfiguration] = None,
    1:     ) -> None:
               """[Internal] Modify a collection by UUID. Can update the name and/or metadata.
       
               Args:
                   id: The internal UUID of the collection to modify.
                   new_name: The new name of the collection.
                                       If None, the existing name will remain. Defaults to None.
                   new_metadata: The new metadata to associate with the collection.
                                             Defaults to None.
                   new_configuration: The new configuration to associate with the collection.
                                             Defaults to None.
               """
               pass
       
    2:     @abstractmethod
    4:     def delete_collection(
               self,
    1:         name: str,
    1:     ) -> None:
               """Delete a collection with the given name.
               Args:
                   name: The name of the collection to delete.
       
               Raises:
                   ValueError: If the collection does not exist.
       
               Examples:
                   ```python
                   client.delete_collection("my_collection")
                   ```
               """
               pass
       
           #
           # ITEM METHODS
           #
       
    2:     @abstractmethod
    9:     def _add(
               self,
    1:         ids: IDs,
    1:         collection_id: UUID,
    1:         embeddings: Embeddings,
    2:         metadatas: Optional[Metadatas] = None,
    2:         documents: Optional[Documents] = None,
    2:         uris: Optional[URIs] = None,
    1:     ) -> bool:
               """[Internal] Add embeddings to a collection specified by UUID.
               If (some) ids already exist, only the new embeddings will be added.
       
               Args:
                   ids: The ids to associate with the embeddings.
                   collection_id: The UUID of the collection to add the embeddings to.
                   embedding: The sequence of embeddings to add.
                   metadata: The metadata to associate with the embeddings. Defaults to None.
                   documents: The documents to associate with the embeddings. Defaults to None.
                   uris: URIs of data sources for each embedding. Defaults to None.
       
               Returns:
                   True if the embeddings were added successfully.
               """
               pass
       
    2:     @abstractmethod
    9:     def _update(
               self,
    1:         collection_id: UUID,
    1:         ids: IDs,
    2:         embeddings: Optional[Embeddings] = None,
    2:         metadatas: Optional[Metadatas] = None,
    2:         documents: Optional[Documents] = None,
    2:         uris: Optional[URIs] = None,
    1:     ) -> bool:
               """[Internal] Update entries in a collection specified by UUID.
       
               Args:
                   collection_id: The UUID of the collection to update the embeddings in.
                   ids: The IDs of the entries to update.
                   embeddings: The sequence of embeddings to update. Defaults to None.
                   metadatas: The metadata to associate with the embeddings. Defaults to None.
                   documents: The documents to associate with the embeddings. Defaults to None.
                   uris: URIs of data sources for each embedding. Defaults to None.
               Returns:
                   True if the embeddings were updated successfully.
               """
               pass
       
    2:     @abstractmethod
    9:     def _upsert(
               self,
    1:         collection_id: UUID,
    1:         ids: IDs,
    1:         embeddings: Embeddings,
    2:         metadatas: Optional[Metadatas] = None,
    2:         documents: Optional[Documents] = None,
    2:         uris: Optional[URIs] = None,
    1:     ) -> bool:
               """[Internal] Add or update entries in the a collection specified by UUID.
               If an entry with the same id already exists, it will be updated,
               otherwise it will be added.
       
               Args:
                   collection_id: The collection to add the embeddings to
                   ids: The ids to associate with the embeddings. Defaults to None.
                   embeddings: The sequence of embeddings to add
                   metadatas: The metadata to associate with the embeddings. Defaults to None.
                   documents: The documents to associate with the embeddings. Defaults to None.
                   uris: URIs of data sources for each embedding. Defaults to None.
               """
               pass
       
    2:     @abstractmethod
    2:     def _count(self, collection_id: UUID) -> int:
               """[Internal] Returns the number of entries in a collection specified by UUID.
       
               Args:
                   collection_id: The UUID of the collection to count the embeddings in.
       
               Returns:
                   int: The number of embeddings in the collection
       
               """
               pass
       
    2:     @abstractmethod
    2:     def _peek(self, collection_id: UUID, n: int = 10) -> GetResult:
               """[Internal] Returns the first n entries in a collection specified by UUID.
       
               Args:
                   collection_id: The UUID of the collection to peek into.
                   n: The number of entries to peek. Defaults to 10.
       
               Returns:
                   GetResult: The first n entries in the collection.
       
               """
       
               pass
       
    2:     @abstractmethod
   10:     def _get(
               self,
    1:         collection_id: UUID,
    2:         ids: Optional[IDs] = None,
    2:         where: Optional[Where] = None,
    2:         limit: Optional[int] = None,
    2:         offset: Optional[int] = None,
    2:         where_document: Optional[WhereDocument] = None,
    2:         include: Include = IncludeMetadataDocuments,
    1:     ) -> GetResult:
               """[Internal] Returns entries from a collection specified by UUID.
       
               Args:
                   ids: The IDs of the entries to get. Defaults to None.
                   where: Conditional filtering on metadata. Defaults to None.
                   limit: The maximum number of entries to return. Defaults to None.
                   offset: The number of entries to skip before returning. Defaults to None.
                   where_document: Conditional filtering on documents. Defaults to None.
                   include: The fields to include in the response.
                                 Defaults to ["metadatas", "documents"].
               Returns:
                   GetResult: The entries in the collection that match the query.
       
               """
               pass
       
    2:     @abstractmethod
    8:     def _delete(
               self,
    1:         collection_id: UUID,
    1:         ids: Optional[IDs],
    2:         where: Optional[Where] = None,
    2:         where_document: Optional[WhereDocument] = None,
    2:         limit: Optional[int] = None,
    1:     ) -> DeleteResult:
               """[Internal] Deletes entries from a collection specified by UUID.
       
               Args:
                   collection_id: The UUID of the collection to delete the entries from.
                   ids: The IDs of the entries to delete. Defaults to None.
                   where: Conditional filtering on metadata. Defaults to None.
                   where_document: Conditional filtering on documents. Defaults to None.
                   limit: Maximum number of records to delete. Can only be used with
                       where or where_document. Defaults to None (no limit).
       
               Returns:
                   DeleteResult: A dict containing the number of records deleted.
               """
               pass
       
    2:     @abstractmethod
   10:     def _query(
               self,
    1:         collection_id: UUID,
    1:         query_embeddings: Embeddings,
    2:         ids: Optional[IDs] = None,
    2:         n_results: int = 10,
    2:         where: Optional[Where] = None,
    2:         where_document: Optional[WhereDocument] = None,
    2:         include: Include = IncludeMetadataDocumentsDistances,
    1:     ) -> QueryResult:
               """[Internal] Performs a nearest neighbors query on a collection specified by UUID.
       
               Args:
                   collection_id: The UUID of the collection to query.
                   query_embeddings: The embeddings to use as the query.
                   ids: The IDs to filter by during the query. Defaults to None.
                   n_results: The number of results to return. Defaults to 10.
                   where: Conditional filtering on metadata. Defaults to None.
                   where_document: Conditional filtering on documents. Defaults to None.
                   include: The fields to include in the response.
                                 Defaults to ["metadatas", "documents", "distances"].
       
               Returns:
                   QueryResult: The results of the query.
               """
               pass
       
    2:     @abstractmethod
    2:     def reset(self) -> bool:
               """Resets the database. This will delete all collections and entries.
       
               Returns:
                   bool: True if the database was reset successfully.
               """
               pass
       
    2:     @abstractmethod
    2:     def get_version(self) -> str:
               """Get the version of Chroma.
       
               Returns:
                   str: The version of Chroma
       
               """
               pass
       
    2:     @abstractmethod
    2:     def get_settings(self) -> Settings:
               """Get the settings used to initialize.
       
               Returns:
                   Settings: The settings used to initialize.
       
               """
               pass
       
    2:     @abstractmethod
    2:     def get_max_batch_size(self) -> int:
               """Return the maximum number of records that can be created or mutated in a single call."""
               pass
       
    2:     @abstractmethod
    2:     def get_user_identity(self) -> UserIdentity:
               """Resolve the tenant and databases for the client. Returns the default
               values if can't be resolved.
       
               """
               pass
       
       
    2: class ClientAPI(BaseAPI, ABC):
    1:     tenant: str
    1:     database: str
       
    2:     @abstractmethod
    5:     def list_collections(
               self,
    2:         limit: Optional[int] = None,
    2:         offset: Optional[int] = None,
    1:     ) -> Sequence[Collection]:
               """List all collections.
               Args:
                   limit: The maximum number of entries to return. Defaults to None.
                   offset: The number of entries to skip before returning. Defaults to None.
       
               Returns:
                   Sequence[Collection]: A list of collections
       
               Examples:
                   ```python
                   client.list_collections()
                   # [collection(name="my_collection", metadata={})]
                   ```
               """
               pass
       
    2:     @abstractmethod
   10:     def create_collection(
               self,
    1:         name: str,
    2:         schema: Optional[Schema] = None,
    2:         configuration: Optional[CreateCollectionConfiguration] = None,
    2:         metadata: Optional[CollectionMetadata] = None,
    2:         embedding_function: Optional[
    1:             EmbeddingFunction[Embeddable]
    1:         ] = DefaultEmbeddingFunction(),  # type: ignore
    2:         data_loader: Optional[DataLoader[Loadable]] = None,
    2:         get_or_create: bool = False,
    1:     ) -> Collection:
               """Create a new collection with the given name and metadata.
               Args:
                   name: The name of the collection to create.
                   metadata: Optional metadata to associate with the collection.
                   embedding_function: Optional function to use to embed documents.
                                       Uses the default embedding function if not provided.
                   get_or_create: If True, return the existing collection if it exists.
                   data_loader: Optional function to use to load records (documents, images, etc.)
       
               Returns:
                   Collection: The newly created collection.
       
               Raises:
                   ValueError: If the collection already exists and get_or_create is False.
                   ValueError: If the collection name is invalid.
       
               Examples:
                   ```python
                   client.create_collection("my_collection")
                   # collection(name="my_collection", metadata={})
       
                   client.create_collection("my_collection", metadata={"foo": "bar"})
                   # collection(name="my_collection", metadata={"foo": "bar"})
                   ```
               """
               pass
       
    2:     @abstractmethod
    6:     def get_collection(
               self,
    1:         name: str,
    2:         embedding_function: Optional[
    1:             EmbeddingFunction[Embeddable]
    1:         ] = DefaultEmbeddingFunction(),  # type: ignore
    2:         data_loader: Optional[DataLoader[Loadable]] = None,
    1:     ) -> Collection:
               """Get a collection with the given name.
               Args:
                   name: The name of the collection to get
                   embedding_function: Optional function to use to embed documents.
                                       Uses the default embedding function if not provided.
                   data_loader: Optional function to use to load records (documents, images, etc.)
       
               Returns:
                   Collection: The collection
       
               Raises:
                   ValueError: If the collection does not exist
       
               Examples:
                   ```python
                   client.get_collection("my_collection")
                   # collection(name="my_collection", metadata={})
                   ```
               """
               pass
       
    2:     @abstractmethod
    6:     def get_collection_by_id(
               self,
    1:         id: UUID,
    2:         embedding_function: Optional[
    1:             EmbeddingFunction[Embeddable]
    1:         ] = DefaultEmbeddingFunction(),  # type: ignore
    2:         data_loader: Optional[DataLoader[Loadable]] = None,
    1:     ) -> Collection:
               """Get a collection by its ID.
       
               Args:
                   id: The UUID of the collection to get.
                   embedding_function: Optional function to use to embed documents.
                                       Uses the default embedding function if not provided.
                   data_loader: Optional function to use to load records (documents, images, etc.)
       
               Returns:
                   Collection: The collection
       
               Raises:
                   NotFoundError: If no collection with the given ID exists.
       
               Examples:
                   ```python
                   client.get_collection_by_id(uuid.UUID("..."))
                   # collection(name="my_collection", metadata={})
                   ```
               """
               pass
       
    2:     @abstractmethod
    9:     def get_or_create_collection(
               self,
    1:         name: str,
    2:         schema: Optional[Schema] = None,
    2:         configuration: Optional[CreateCollectionConfiguration] = None,
    2:         metadata: Optional[CollectionMetadata] = None,
    2:         embedding_function: Optional[
    1:             EmbeddingFunction[Embeddable]
    1:         ] = DefaultEmbeddingFunction(),  # type: ignore
    2:         data_loader: Optional[DataLoader[Loadable]] = None,
    1:     ) -> Collection:
               """Get or create a collection with the given name and metadata.
               Args:
                   name: The name of the collection to get or create
                   metadata: Optional metadata to associate with the collection. If
                   the collection already exists, the metadata provided is ignored.
                   If the collection does not exist, the new collection will be created
                   with the provided metadata.
                   embedding_function: Optional function to use to embed documents
                   data_loader: Optional function to use to load records (documents, images, etc.)
       
               Returns:
                   The collection
       
               Examples:
                   ```python
                   client.get_or_create_collection("my_collection")
                   # collection(name="my_collection", metadata={})
                   ```
               """
               pass
       
    2:     @abstractmethod
    2:     def set_tenant(self, tenant: str, database: str = DEFAULT_DATABASE) -> None:
               """Set the tenant and database for the client. Raises an error if the tenant or
               database does not exist.
       
               Args:
                   tenant: The tenant to set.
                   database: The database to set.
       
               """
               pass
       
    2:     @abstractmethod
    2:     def set_database(self, database: str) -> None:
               """Set the database for the client. Raises an error if the database does not exist.
       
               Args:
                   database: The database to set.
       
               """
               pass
       
    2:     @staticmethod
    2:     @abstractmethod
    2:     def clear_system_cache() -> None:
               """Clear the system cache so that new systems can be created for an existing path.
               This should only be used for testing purposes."""
               pass
       
       
    2: class AdminAPI(ABC):
    2:     @abstractmethod
    2:     def create_database(self, name: str, tenant: str = DEFAULT_TENANT) -> None:
               """Create a new database. Raises an error if the database already exists.
       
               Args:
                   database: The name of the database to create.
       
               """
               pass
       
    2:     @abstractmethod
    2:     def get_database(self, name: str, tenant: str = DEFAULT_TENANT) -> Database:
               """Get a database. Raises an error if the database does not exist.
       
               Args:
                   database: The name of the database to get.
                   tenant: The tenant of the database to get.
       
               """
               pass
       
    2:     @abstractmethod
    2:     def delete_database(self, name: str, tenant: str = DEFAULT_TENANT) -> None:
               """Delete a database. Raises an error if the database does not exist.
       
               Args:
                   database: The name of the database to delete.
                   tenant: The tenant of the database to delete.
       
               """
               pass
       
    2:     @abstractmethod
    6:     def list_databases(
               self,
    2:         limit: Optional[int] = None,
    2:         offset: Optional[int] = None,
    2:         tenant: str = DEFAULT_TENANT,
    1:     ) -> Sequence[Database]:
               """List all databases for a tenant. Raises an error if the tenant does not exist.
       
               Args:
                   tenant: The tenant to list databases for.
       
               """
               pass
       
    2:     @abstractmethod
    2:     def create_tenant(self, name: str) -> None:
               """Create a new tenant. Raises an error if the tenant already exists.
       
               Args:
                   tenant: The name of the tenant to create.
       
               """
               pass
       
    2:     @abstractmethod
    2:     def get_tenant(self, name: str) -> Tenant:
               """Get a tenant. Raises an error if the tenant does not exist.
       
               Args:
                   tenant: The name of the tenant to get.
       
               """
               pass
       
       
    2: class ServerAPI(BaseAPI, AdminAPI, Component):
    1:     """An API instance that extends the relevant Base API methods by passing
           in a tenant and database. This is the root component of the Chroma System"""
       
    2:     @abstractmethod
    2:     @override
    5:     def count_collections(
    3:         self, tenant: str = DEFAULT_TENANT, database: str = DEFAULT_DATABASE
    1:     ) -> int:
               pass
       
    2:     @abstractmethod
    7:     def list_collections(
               self,
    2:         limit: Optional[int] = None,
    2:         offset: Optional[int] = None,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> Sequence[CollectionModel]:
               pass
       
    2:     @abstractmethod
   10:     def create_collection(
               self,
    1:         name: str,
    2:         schema: Optional[Schema] = None,
    2:         configuration: Optional[CreateCollectionConfiguration] = None,
    2:         metadata: Optional[CollectionMetadata] = None,
    2:         get_or_create: bool = False,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> CollectionModel:
               pass
       
    2:     @abstractmethod
    6:     def get_collection(
               self,
    1:         name: str,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> CollectionModel:
               pass
       
    2:     @abstractmethod
    6:     def get_collection_by_id(
               self,
    1:         collection_id: UUID,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> CollectionModel:
               """Get a collection by its ID.
       
               Args:
                   collection_id: The UUID of the collection to retrieve.
                   tenant: The tenant to search within.
                   database: The database to search within.
       
               Returns:
                   CollectionModel: The collection with the given ID.
       
               Raises:
                   NotFoundError: If no collection with the given ID exists.
               """
               pass
       
    2:     @abstractmethod
    9:     def get_or_create_collection(
               self,
    1:         name: str,
    2:         schema: Optional[Schema] = None,
    2:         configuration: Optional[CreateCollectionConfiguration] = None,
    2:         metadata: Optional[CollectionMetadata] = None,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> CollectionModel:
               pass
       
    2:     @abstractmethod
    2:     @override
    6:     def delete_collection(
               self,
    1:         name: str,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> None:
               pass
       
    2:     @abstractmethod
    2:     @override
    9:     def _modify(
               self,
    1:         id: UUID,
    2:         new_name: Optional[str] = None,
    2:         new_metadata: Optional[CollectionMetadata] = None,
    2:         new_configuration: Optional[UpdateCollectionConfiguration] = None,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> None:
               pass
       
    2:     @abstractmethod
    7:     def _fork(
               self,
    1:         collection_id: UUID,
    1:         new_name: str,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> CollectionModel:
               pass
       
    2:     @abstractmethod
    6:     def _fork_count(
               self,
    1:         collection_id: UUID,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> int:
               pass
       
    2:     @abstractmethod
    6:     def _get_indexing_status(
               self,
    1:         collection_id: UUID,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> "IndexingStatus":
               pass
       
    2:     @abstractmethod
    8:     def _search(
               self,
    1:         collection_id: UUID,
    1:         searches: List[Search],
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    2:         read_level: ReadLevel = ReadLevel.INDEX_AND_WAL,
    1:     ) -> SearchResult:
               pass
       
    2:     @abstractmethod
    2:     @override
    7:     def _count(
               self,
    1:         collection_id: UUID,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    2:         read_level: ReadLevel = ReadLevel.INDEX_AND_WAL,
    1:     ) -> int:
               pass
       
    2:     @abstractmethod
    2:     @override
    7:     def _peek(
               self,
    1:         collection_id: UUID,
    2:         n: int = 10,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> GetResult:
               pass
       
    2:     @abstractmethod
    2:     @override
   12:     def _get(
               self,
    1:         collection_id: UUID,
    2:         ids: Optional[IDs] = None,
    2:         where: Optional[Where] = None,
    2:         limit: Optional[int] = None,
    2:         offset: Optional[int] = None,
    2:         where_document: Optional[WhereDocument] = None,
    2:         include: Include = IncludeMetadataDocuments,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> GetResult:
               pass
       
    2:     @abstractmethod
    2:     @override
   11:     def _add(
               self,
    1:         ids: IDs,
    1:         collection_id: UUID,
    1:         embeddings: Embeddings,
    2:         metadatas: Optional[Metadatas] = None,
    2:         documents: Optional[Documents] = None,
    2:         uris: Optional[URIs] = None,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> bool:
               pass
       
    2:     @abstractmethod
    2:     @override
   11:     def _update(
               self,
    1:         collection_id: UUID,
    1:         ids: IDs,
    2:         embeddings: Optional[Embeddings] = None,
    2:         metadatas: Optional[Metadatas] = None,
    2:         documents: Optional[Documents] = None,
    2:         uris: Optional[URIs] = None,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> bool:
               pass
       
    2:     @abstractmethod
    2:     @override
   11:     def _upsert(
               self,
    1:         collection_id: UUID,
    1:         ids: IDs,
    1:         embeddings: Embeddings,
    2:         metadatas: Optional[Metadatas] = None,
    2:         documents: Optional[Documents] = None,
    2:         uris: Optional[URIs] = None,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> bool:
               pass
       
    2:     @abstractmethod
    2:     @override
   12:     def _query(
               self,
    1:         collection_id: UUID,
    1:         query_embeddings: Embeddings,
    2:         ids: Optional[IDs] = None,
    2:         n_results: int = 10,
    2:         where: Optional[Where] = None,
    2:         where_document: Optional[WhereDocument] = None,
    2:         include: Include = IncludeMetadataDocumentsDistances,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> QueryResult:
               pass
       
    2:     @abstractmethod
    2:     @override
   10:     def _delete(
               self,
    1:         collection_id: UUID,
    2:         ids: Optional[IDs] = None,
    2:         where: Optional[Where] = None,
    2:         where_document: Optional[WhereDocument] = None,
    2:         limit: Optional[int] = None,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> DeleteResult:
               pass
       
    2:     @abstractmethod
   10:     def attach_function(
               self,
    1:         function_id: str,
    1:         name: str,
    1:         input_collection_id: UUID,
    1:         output_collection: str,
    2:         params: Optional[Dict[str, Any]] = None,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> Tuple["AttachedFunction", bool]:
               """Attach a function to a collection.
       
               Args:
                   function_id: Built-in function identifier
                   name: Unique name for this attached function
                   input_collection_id: Source collection that triggers the function
                   output_collection: Target collection where function output is stored
                   params: Optional dictionary with function-specific parameters
                   tenant: The tenant name
                   database: The database name
       
               Returns:
                   Tuple of (AttachedFunction, created) where created is True if newly created,
                   False if already existed (idempotent request)
               """
               pass
       
    2:     @abstractmethod
    7:     def get_attached_function(
               self,
    1:         name: str,
    1:         input_collection_id: UUID,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> "AttachedFunction":
               """Get an attached function by name for a specific collection.
       
               Args:
                   name: Name of the attached function
                   input_collection_id: The collection ID
                   tenant: The tenant name
                   database: The database name
       
               Returns:
                   AttachedFunction: The attached function object
       
               Raises:
                   NotFoundError: If the attached function doesn't exist
               """
               pass
       
    2:     @abstractmethod
    8:     def detach_function(
               self,
    1:         name: str,
    1:         input_collection_id: UUID,
    2:         delete_output: bool = False,
    2:         tenant: str = DEFAULT_TENANT,
    2:         database: str = DEFAULT_DATABASE,
    1:     ) -> bool:
               """Detach a function and prevent any further runs.
       
               Args:
                   name: Name of the attached function to remove
                   input_collection_id: ID of the input collection
                   delete_output: Whether to also delete the output collection
                   tenant: The tenant name
                   database: The database name
       
               Returns:
                   bool: True if successful
               """
               pass
