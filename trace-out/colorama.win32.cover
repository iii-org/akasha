       # Copyright Jonathan Hartley 2013. BSD 3-Clause license, see LICENSE file.
       
       # from winbase.h
    1: STDOUT = -11
    1: STDERR = -12
       
    1: ENABLE_VIRTUAL_TERMINAL_PROCESSING = 0x0004
       
    1: try:
    1:     import ctypes
    1:     from ctypes import LibraryLoader
    1:     windll = LibraryLoader(ctypes.WinDLL)
    1:     from ctypes import wintypes
       except (AttributeError, ImportError):
           windll = None
           SetConsoleTextAttribute = lambda *_: None
           winapi_test = lambda *_: None
       else:
    1:     from ctypes import byref, Structure, c_char, POINTER
       
    1:     COORD = wintypes._COORD
       
    2:     class CONSOLE_SCREEN_BUFFER_INFO(Structure):
    1:         """struct in wincon.h."""
    1:         _fields_ = [
    1:             ("dwSize", COORD),
    1:             ("dwCursorPosition", COORD),
    1:             ("wAttributes", wintypes.WORD),
    1:             ("srWindow", wintypes.SMALL_RECT),
    1:             ("dwMaximumWindowSize", COORD),
               ]
    1:         def __str__(self):
                   return '(%d,%d,%d,%d,%d,%d,%d,%d,%d,%d,%d)' % (
                       self.dwSize.Y, self.dwSize.X
                       , self.dwCursorPosition.Y, self.dwCursorPosition.X
                       , self.wAttributes
                       , self.srWindow.Top, self.srWindow.Left, self.srWindow.Bottom, self.srWindow.Right
                       , self.dwMaximumWindowSize.Y, self.dwMaximumWindowSize.X
                   )
       
    1:     _GetStdHandle = windll.kernel32.GetStdHandle
    1:     _GetStdHandle.argtypes = [
    1:         wintypes.DWORD,
           ]
    1:     _GetStdHandle.restype = wintypes.HANDLE
       
    1:     _GetConsoleScreenBufferInfo = windll.kernel32.GetConsoleScreenBufferInfo
    1:     _GetConsoleScreenBufferInfo.argtypes = [
    1:         wintypes.HANDLE,
    1:         POINTER(CONSOLE_SCREEN_BUFFER_INFO),
           ]
    1:     _GetConsoleScreenBufferInfo.restype = wintypes.BOOL
       
    1:     _SetConsoleTextAttribute = windll.kernel32.SetConsoleTextAttribute
    1:     _SetConsoleTextAttribute.argtypes = [
    1:         wintypes.HANDLE,
    1:         wintypes.WORD,
           ]
    1:     _SetConsoleTextAttribute.restype = wintypes.BOOL
       
    1:     _SetConsoleCursorPosition = windll.kernel32.SetConsoleCursorPosition
    1:     _SetConsoleCursorPosition.argtypes = [
    1:         wintypes.HANDLE,
    1:         COORD,
           ]
    1:     _SetConsoleCursorPosition.restype = wintypes.BOOL
       
    1:     _FillConsoleOutputCharacterA = windll.kernel32.FillConsoleOutputCharacterA
    1:     _FillConsoleOutputCharacterA.argtypes = [
    1:         wintypes.HANDLE,
    1:         c_char,
    1:         wintypes.DWORD,
    1:         COORD,
    1:         POINTER(wintypes.DWORD),
           ]
    1:     _FillConsoleOutputCharacterA.restype = wintypes.BOOL
       
    1:     _FillConsoleOutputAttribute = windll.kernel32.FillConsoleOutputAttribute
    1:     _FillConsoleOutputAttribute.argtypes = [
    1:         wintypes.HANDLE,
    1:         wintypes.WORD,
    1:         wintypes.DWORD,
    1:         COORD,
    1:         POINTER(wintypes.DWORD),
           ]
    1:     _FillConsoleOutputAttribute.restype = wintypes.BOOL
       
    1:     _SetConsoleTitleW = windll.kernel32.SetConsoleTitleW
    1:     _SetConsoleTitleW.argtypes = [
    1:         wintypes.LPCWSTR
           ]
    1:     _SetConsoleTitleW.restype = wintypes.BOOL
       
    1:     _GetConsoleMode = windll.kernel32.GetConsoleMode
    1:     _GetConsoleMode.argtypes = [
    1:         wintypes.HANDLE,
    1:         POINTER(wintypes.DWORD)
           ]
    1:     _GetConsoleMode.restype = wintypes.BOOL
       
    1:     _SetConsoleMode = windll.kernel32.SetConsoleMode
    1:     _SetConsoleMode.argtypes = [
    1:         wintypes.HANDLE,
    1:         wintypes.DWORD
           ]
    1:     _SetConsoleMode.restype = wintypes.BOOL
       
    1:     def _winapi_test(handle):
    8:         csbi = CONSOLE_SCREEN_BUFFER_INFO()
   16:         success = _GetConsoleScreenBufferInfo(
    8:             handle, byref(csbi))
    8:         return bool(success)
       
    1:     def winapi_test():
   20:         return any(_winapi_test(h) for h in
    4:                    (_GetStdHandle(STDOUT), _GetStdHandle(STDERR)))
       
    1:     def GetConsoleScreenBufferInfo(stream_id=STDOUT):
    1:         handle = _GetStdHandle(stream_id)
    1:         csbi = CONSOLE_SCREEN_BUFFER_INFO()
    2:         success = _GetConsoleScreenBufferInfo(
    1:             handle, byref(csbi))
    1:         return csbi
       
    1:     def SetConsoleTextAttribute(stream_id, attrs):
               handle = _GetStdHandle(stream_id)
               return _SetConsoleTextAttribute(handle, attrs)
       
    1:     def SetConsoleCursorPosition(stream_id, position, adjust=True):
               position = COORD(*position)
               # If the position is out of range, do nothing.
               if position.Y <= 0 or position.X <= 0:
                   return
               # Adjust for Windows' SetConsoleCursorPosition:
               #    1. being 0-based, while ANSI is 1-based.
               #    2. expecting (x,y), while ANSI uses (y,x).
               adjusted_position = COORD(position.Y - 1, position.X - 1)
               if adjust:
                   # Adjust for viewport's scroll position
                   sr = GetConsoleScreenBufferInfo(STDOUT).srWindow
                   adjusted_position.Y += sr.Top
                   adjusted_position.X += sr.Left
               # Resume normal processing
               handle = _GetStdHandle(stream_id)
               return _SetConsoleCursorPosition(handle, adjusted_position)
       
    1:     def FillConsoleOutputCharacter(stream_id, char, length, start):
               handle = _GetStdHandle(stream_id)
               char = c_char(char.encode())
               length = wintypes.DWORD(length)
               num_written = wintypes.DWORD(0)
               # Note that this is hard-coded for ANSI (vs wide) bytes.
               success = _FillConsoleOutputCharacterA(
                   handle, char, length, start, byref(num_written))
               return num_written.value
       
    1:     def FillConsoleOutputAttribute(stream_id, attr, length, start):
               ''' FillConsoleOutputAttribute( hConsole, csbi.wAttributes, dwConSize, coordScreen, &cCharsWritten )'''
               handle = _GetStdHandle(stream_id)
               attribute = wintypes.WORD(attr)
               length = wintypes.DWORD(length)
               num_written = wintypes.DWORD(0)
               # Note that this is hard-coded for ANSI (vs wide) bytes.
               return _FillConsoleOutputAttribute(
                   handle, attribute, length, start, byref(num_written))
       
    1:     def SetConsoleTitle(title):
               return _SetConsoleTitleW(title)
       
    1:     def GetConsoleMode(handle):
               mode = wintypes.DWORD()
               success = _GetConsoleMode(handle, byref(mode))
               if not success:
                   raise ctypes.WinError()
               return mode.value
       
    1:     def SetConsoleMode(handle, mode):
               success = _SetConsoleMode(handle, mode)
               if not success:
                   raise ctypes.WinError()
