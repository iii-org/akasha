       # File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
       
    1: from typing import Dict, Optional
    1: from typing_extensions import Literal
       
    1: from pydantic import Field as FieldInfo
       
    1: from ..._models import BaseModel
       
    1: __all__ = ["ResponseFormatJSONSchema", "JSONSchema"]
       
       
    2: class JSONSchema(BaseModel):
    1:     """Structured Outputs configuration options, including a JSON Schema."""
       
    1:     name: str
    1:     """The name of the response format.
       
           Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length
           of 64.
           """
       
    1:     description: Optional[str] = None
    1:     """
           A description of what the response format is for, used by the model to determine
           how to respond in the format.
           """
       
    1:     schema_: Optional[Dict[str, object]] = FieldInfo(alias="schema", default=None)
    1:     """
           The schema for the response format, described as a JSON Schema object. Learn how
           to build JSON schemas [here](https://json-schema.org/).
           """
       
    1:     strict: Optional[bool] = None
    1:     """
           Whether to enable strict schema adherence when generating the output. If set to
           true, the model will always follow the exact schema defined in the `schema`
           field. Only a subset of JSON Schema is supported when `strict` is `true`. To
           learn more, read the
           [Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs).
           """
       
       
    2: class ResponseFormatJSONSchema(BaseModel):
    1:     """JSON Schema response format.
       
           Used to generate structured JSON responses.
           Learn more about [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs).
           """
       
    1:     json_schema: JSONSchema
    1:     """Structured Outputs configuration options, including a JSON Schema."""
       
    1:     type: Literal["json_schema"]
    1:     """The type of response format being defined. Always `json_schema`."""
