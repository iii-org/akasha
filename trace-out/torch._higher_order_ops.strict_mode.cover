       # mypy: allow-untyped-defs
    1: from typing import Any, TYPE_CHECKING, Union
       
    1: import torch
    1: import torch._subclasses.functional_tensor
    1: import torch.utils._pytree as pytree
    1: from torch._C import DispatchKey
    1: from torch._functorch.utils import exposed_in
    1: from torch._higher_order_ops.utils import _set_compilation_env, autograd_not_implemented
    1: from torch._ops import HigherOrderOperator
    1: from torch._subclasses.fake_tensor import FakeTensorMode
    1: from torch.fx.experimental.proxy_tensor import (
           _temp_remove_metadata_torch_function_mode,
           _temp_remove_pre_dispatch_torch_function_mode,
           disable_proxy_modes_tracing,
           make_fx,
           ProxyTorchDispatchMode,
           track_tensor_tree,
       )
    1: from torch.utils._python_dispatch import _get_current_dispatch_mode
       
       
    1: if TYPE_CHECKING:
           from collections.abc import Callable
       
       
    2: @exposed_in("torch")
    2: def strict_mode(callable, operands):
           from torch._dynamo.backends.debugging import (
               make_eager_backend_with_torch_function_modes,
           )
       
           if torch.compiler.is_dynamo_compiling():
               return strict_mode_op(callable, operands)
       
           with _set_compilation_env():
               with _temp_remove_metadata_torch_function_mode() as metadata_mode:
                   with _temp_remove_pre_dispatch_torch_function_mode() as predispatch_mode:
                       modes = [metadata_mode, predispatch_mode]
                       modes = [mode for mode in modes if mode is not None]
                       if modes:
                           backend: Union[str, Callable[..., Any]] = (
                               make_eager_backend_with_torch_function_modes(modes)
                           )
                       else:
                           backend = "eager"
                       with torch._dynamo.utils.disable_cache_limit():
                           return torch.compile(
                               strict_mode_op, backend=backend, fullgraph=True
                           )(callable, operands)
       
       
    2: class StrictMode(HigherOrderOperator):
    1:     def __init__(self):
    1:         super().__init__("strict_mode")
       
    1:     def __call__(self, callable, operands):
               return super().__call__(callable, operands)
       
       
    1: strict_mode_op = StrictMode()
       
       
    2: @strict_mode_op.py_impl(DispatchKey.CompositeExplicitAutograd)
    2: def strict_mode_op_dense(callable, operands):
           mode = _get_current_dispatch_mode()
           assert mode is None, "Mode should never be enabled for CPU/CUDA key"
           return callable(*operands)
       
       
    2: strict_mode_op.py_autograd_impl(
    1:     autograd_not_implemented(strict_mode_op, deferred_error=True)
       )
       
       
    2: @strict_mode_op.py_impl(ProxyTorchDispatchMode)
    2: def inner(mode, callable, operands):
           return trace_strict_mode(mode, strict_mode_op, callable, operands)
       
       
    1: def trace_strict_mode(mode, strict_mode_op, callable, operands):
           pre_dispatch = getattr(mode, "pre_dispatch", False)
       
           with disable_proxy_modes_tracing():
               graph = make_fx(callable, pre_dispatch=pre_dispatch)(*operands)
       
           graph_name = mode.tracer.get_fresh_qualname("strict_graph_")
           mode.tracer.root.register_module(graph_name, graph)
       
           args = (graph, operands)
       
           proxy_args = pytree.tree_map(mode.tracer.unwrap_proxy, args)
       
           out_proxy = mode.tracer.create_proxy(
               "call_function", strict_mode_op, proxy_args, {}, name="strict_mode"
           )
       
           out = graph(*operands)
           return track_tensor_tree(out, out_proxy, constant=None, tracer=mode.tracer)
       
       
    2: @strict_mode_op.py_impl(FakeTensorMode)
    2: def strict_mode_fake_tensor_mode(mode, callable, operands):
           with mode:
               true_outs = callable(*operands)
           return true_outs
       
       
    2: @strict_mode_op.py_functionalize_impl
    2: def strict_mode_func(ctx, callable, inputs):
           unwrapped_inputs = ctx.unwrap_tensors(inputs)
           with ctx.redispatch_to_next():
               functional_callable = ctx.functionalize(callable)
       
               cond_return = strict_mode_op(functional_callable, unwrapped_inputs)
               return ctx.wrap_tensors(cond_return)
