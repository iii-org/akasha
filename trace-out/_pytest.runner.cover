       # mypy: allow-untyped-defs
    1: """Basic collect and runtest protocol implementations."""
       
    1: from __future__ import annotations
       
    1: import bdb
    1: from collections.abc import Callable
    1: import dataclasses
    1: import os
    1: import sys
    1: import types
    1: from typing import cast
    1: from typing import final
    1: from typing import Generic
    1: from typing import Literal
    1: from typing import TYPE_CHECKING
    1: from typing import TypeVar
       
    1: from .config import Config
    1: from .reports import BaseReport
    1: from .reports import CollectErrorRepr
    1: from .reports import CollectReport
    1: from .reports import TestReport
    1: from _pytest import timing
    1: from _pytest._code.code import ExceptionChainRepr
    1: from _pytest._code.code import ExceptionInfo
    1: from _pytest._code.code import TerminalRepr
    1: from _pytest.config.argparsing import Parser
    1: from _pytest.deprecated import check_ispytest
    1: from _pytest.nodes import Collector
    1: from _pytest.nodes import Directory
    1: from _pytest.nodes import Item
    1: from _pytest.nodes import Node
    1: from _pytest.outcomes import Exit
    1: from _pytest.outcomes import OutcomeException
    1: from _pytest.outcomes import Skipped
    1: from _pytest.outcomes import TEST_OUTCOME
       
       
    1: if sys.version_info < (3, 11):
           from exceptiongroup import BaseExceptionGroup
       
    1: if TYPE_CHECKING:
           from _pytest.main import Session
           from _pytest.terminal import TerminalReporter
       
       #
       # pytest plugin hooks.
       
       
    1: def pytest_addoption(parser: Parser) -> None:
    1:     group = parser.getgroup("terminal reporting", "Reporting", after="general")
    2:     group.addoption(
    1:         "--durations",
    1:         action="store",
    1:         type=int,
    1:         default=None,
    1:         metavar="N",
    1:         help="Show N slowest setup/test durations (N=0 for all)",
           )
    2:     group.addoption(
    1:         "--durations-min",
    1:         action="store",
    1:         type=float,
    1:         default=None,
    1:         metavar="N",
    1:         help="Minimal duration in seconds for inclusion in slowest list. "
               "Default: 0.005 (or 0.0 if -vv is given).",
           )
       
       
    1: def pytest_terminal_summary(terminalreporter: TerminalReporter) -> None:
    1:     durations = terminalreporter.config.option.durations
    1:     durations_min = terminalreporter.config.option.durations_min
    1:     verbose = terminalreporter.config.get_verbosity()
    1:     if durations is None:
    1:         return
           if durations_min is None:
               durations_min = 0.005 if verbose < 2 else 0.0
           tr = terminalreporter
           dlist = []
           for replist in tr.stats.values():
               for rep in replist:
                   if hasattr(rep, "duration"):
                       dlist.append(rep)
           if not dlist:
               return
           dlist.sort(key=lambda x: x.duration, reverse=True)
           if not durations:
               tr.write_sep("=", "slowest durations")
           else:
               tr.write_sep("=", f"slowest {durations} durations")
               dlist = dlist[:durations]
       
           for i, rep in enumerate(dlist):
               if rep.duration < durations_min:
                   tr.write_line("")
                   message = f"({len(dlist) - i} durations < {durations_min:g}s hidden."
                   if terminalreporter.config.option.durations_min is None:
                       message += "  Use -vv to show these durations."
                   message += ")"
                   tr.write_line(message)
                   break
               tr.write_line(f"{rep.duration:02.2f}s {rep.when:<8} {rep.nodeid}")
       
       
    1: def pytest_sessionstart(session: Session) -> None:
    1:     session._setupstate = SetupState()
       
       
    1: def pytest_sessionfinish(session: Session) -> None:
    1:     session._setupstate.teardown_exact(None)
       
       
    1: def pytest_runtest_protocol(item: Item, nextitem: Item | None) -> bool:
    6:     ihook = item.ihook
    6:     ihook.pytest_runtest_logstart(nodeid=item.nodeid, location=item.location)
    6:     runtestprotocol(item, nextitem=nextitem)
    6:     ihook.pytest_runtest_logfinish(nodeid=item.nodeid, location=item.location)
    6:     return True
       
       
    1: def runtestprotocol(
    1:     item: Item, log: bool = True, nextitem: Item | None = None
       ) -> list[TestReport]:
    6:     hasrequest = hasattr(item, "_request")
    6:     if hasrequest and not item._request:  # type: ignore[attr-defined]
               # This only happens if the item is re-run, as is done by
               # pytest-rerunfailures.
               item._initrequest()  # type: ignore[attr-defined]
    6:     try:
    6:         rep = call_and_report(item, "setup", log)
    6:         reports = [rep]
    6:         if rep.passed:
    6:             setup_only = item.config.getoption("setuponly", False)
    6:             if item.config.getoption("setupshow", False):
                       show_test_item(item, add_space=not setup_only)
    6:             if not setup_only:
    6:                 reports.append(call_and_report(item, "call", log))
               # If the session is about to fail or stop, teardown everything - this is
               # necessary to correctly report fixture teardown errors (see #11706)
    6:         if item.session.shouldfail or item.session.shouldstop:
                   nextitem = None
    6:         reports.append(call_and_report(item, "teardown", log, nextitem=nextitem))
           finally:
               # After all teardown hooks have been called (or an exception was reraised)
               # want funcargs and request info to go away.
    6:         if hasrequest:
    6:             item._request = False  # type: ignore[attr-defined]
    6:             item.funcargs = None  # type: ignore[attr-defined]
    6:     return reports
       
       
    1: def show_test_item(item: Item, *, add_space: bool) -> None:
           """Show test function, parameters and the fixtures of the test item."""
           tw = item.config.get_terminal_writer()
           tw.line()
           tw.write(" " * 8)
           tw.write(item.nodeid)
           used_fixtures = sorted(getattr(item, "fixturenames", []))
           if used_fixtures:
               tw.write(f" (fixtures used: {', '.join(used_fixtures)})")
           if add_space:
               tw.write(" ")
           tw.flush()
       
       
    1: def pytest_runtest_setup(item: Item) -> None:
    6:     _update_current_test_var(item, "setup")
    6:     item.session._setupstate.setup(item)
       
       
    1: def pytest_runtest_call(item: Item) -> None:
    6:     _update_current_test_var(item, "call")
    6:     try:
    6:         del sys.last_type
               del sys.last_value
               del sys.last_traceback
               if sys.version_info >= (3, 12, 0):
                   del sys.last_exc  # type:ignore[attr-defined]
    6:     except AttributeError:
    6:         pass
    6:     try:
    6:         item.runtest()
           except Exception as e:
               # Store trace info to allow postmortem debugging
               sys.last_type = type(e)
               sys.last_value = e
               if sys.version_info >= (3, 12, 0):
                   sys.last_exc = e  # type:ignore[attr-defined]
               assert e.__traceback__ is not None
               # Skip *this* frame
               sys.last_traceback = e.__traceback__.tb_next
               raise
       
       
    1: def pytest_runtest_teardown(item: Item, nextitem: Item | None) -> None:
    6:     _update_current_test_var(item, "teardown")
    6:     item.session._setupstate.teardown_exact(nextitem)
    6:     _update_current_test_var(item, None)
       
       
    1: def _update_current_test_var(
           item: Item, when: Literal["setup", "call", "teardown"] | None
       ) -> None:
           """Update :envvar:`PYTEST_CURRENT_TEST` to reflect the current item and stage.
       
           If ``when`` is None, delete ``PYTEST_CURRENT_TEST`` from the environment.
           """
   24:     var_name = "PYTEST_CURRENT_TEST"
   24:     if when:
   18:         value = f"{item.nodeid} ({when})"
               # don't allow null bytes on environment variables (see #2644, #2957)
   18:         value = value.replace("\x00", "(null)")
   18:         os.environ[var_name] = value
           else:
    6:         os.environ.pop(var_name)
       
       
    1: def pytest_report_teststatus(report: BaseReport) -> tuple[str, str, str] | None:
   18:     if report.when in ("setup", "teardown"):
   12:         if report.failed:
                   #      category, shortletter, verbose-word
                   return "error", "E", "ERROR"
   12:         elif report.skipped:
                   return "skipped", "s", "SKIPPED"
               else:
   12:             return "", "", ""
    6:     return None
       
       
       #
       # Implementation
       
       
    1: def call_and_report(
    1:     item: Item, when: Literal["setup", "call", "teardown"], log: bool = True, **kwds
       ) -> TestReport:
   18:     ihook = item.ihook
   18:     if when == "setup":
    6:         runtest_hook: Callable[..., None] = ihook.pytest_runtest_setup
   12:     elif when == "call":
    6:         runtest_hook = ihook.pytest_runtest_call
    6:     elif when == "teardown":
    6:         runtest_hook = ihook.pytest_runtest_teardown
           else:
               assert False, f"Unhandled runtest hook case: {when}"
       
   36:     call = CallInfo.from_call(
   36:         lambda: runtest_hook(item=item, **kwds),
   18:         when=when,
   18:         reraise=get_reraise_exceptions(item.config),
           )
   18:     report: TestReport = ihook.pytest_runtest_makereport(item=item, call=call)
   18:     if log:
   18:         ihook.pytest_runtest_logreport(report=report)
   18:     if check_interactive_exception(call, report):
               ihook.pytest_exception_interact(node=item, call=call, report=report)
   18:     return report
       
       
    1: def get_reraise_exceptions(config: Config) -> tuple[type[BaseException], ...]:
           """Return exception types that should not be suppressed in general."""
   18:     reraise: tuple[type[BaseException], ...] = (Exit,)
   18:     if not config.getoption("usepdb", False):
   18:         reraise += (KeyboardInterrupt,)
   18:     return reraise
       
       
    1: def check_interactive_exception(call: CallInfo[object], report: BaseReport) -> bool:
           """Check whether the call raised an exception that should be reported as
           interactive."""
   23:     if call.excinfo is None:
               # Didn't raise.
   23:         return False
           if hasattr(report, "wasxfail"):
               # Exception was expected.
               return False
           unittest = sys.modules.get("unittest")
           if isinstance(call.excinfo.value, Skipped | bdb.BdbQuit) or (
               unittest is not None and isinstance(call.excinfo.value, unittest.SkipTest)
           ):
               # Special control flow exception.
               return False
           return True
       
       
    1: TResult = TypeVar("TResult", covariant=True)
       
       
    3: @final
    2: @dataclasses.dataclass
    2: class CallInfo(Generic[TResult]):
    1:     """Result/Exception info of a function invocation."""
       
    1:     _result: TResult | None
           #: The captured exception of the call, if it raised.
    1:     excinfo: ExceptionInfo[BaseException] | None
           #: The system time when the call started, in seconds since the epoch.
    1:     start: float
           #: The system time when the call ended, in seconds since the epoch.
    1:     stop: float
           #: The call duration, in seconds.
    1:     duration: float
           #: The context of invocation: "collect", "setup", "call" or "teardown".
    1:     when: Literal["collect", "setup", "call", "teardown"]
       
    1:     def __init__(
               self,
               result: TResult | None,
               excinfo: ExceptionInfo[BaseException] | None,
               start: float,
               stop: float,
               duration: float,
               when: Literal["collect", "setup", "call", "teardown"],
               *,
    1:         _ispytest: bool = False,
           ) -> None:
   23:         check_ispytest(_ispytest)
   23:         self._result = result
   23:         self.excinfo = excinfo
   23:         self.start = start
   23:         self.stop = stop
   23:         self.duration = duration
   23:         self.when = when
       
    2:     @property
    2:     def result(self) -> TResult:
               """The return value of the call, if it didn't raise.
       
               Can only be accessed if excinfo is None.
               """
    5:         if self.excinfo is not None:
                   raise AttributeError(f"{self!r} has no valid result")
               # The cast is safe because an exception wasn't raised, hence
               # _result has the expected function return type (which may be
               #  None, that's why a cast and not an assert).
    5:         return cast(TResult, self._result)
       
    2:     @classmethod
    2:     def from_call(
               cls,
               func: Callable[[], TResult],
               when: Literal["collect", "setup", "call", "teardown"],
    1:         reraise: type[BaseException] | tuple[type[BaseException], ...] | None = None,
           ) -> CallInfo[TResult]:
               """Call func, wrapping the result in a CallInfo.
       
               :param func:
                   The function to call. Called without arguments.
               :type func: Callable[[], _pytest.runner.TResult]
               :param when:
                   The phase in which the function is called.
               :param reraise:
                   Exception or exceptions that shall propagate if raised by the
                   function, instead of being wrapped in the CallInfo.
               """
   23:         excinfo = None
   23:         instant = timing.Instant()
   23:         try:
   23:             result: TResult | None = func()
               except BaseException:
                   excinfo = ExceptionInfo.from_current()
                   if reraise is not None and isinstance(excinfo.value, reraise):
                       raise
                   result = None
   23:         duration = instant.elapsed()
   46:         return cls(
   23:             start=duration.start.time,
   23:             stop=duration.stop.time,
   23:             duration=duration.seconds,
   23:             when=when,
   23:             result=result,
   23:             excinfo=excinfo,
   23:             _ispytest=True,
               )
       
    1:     def __repr__(self) -> str:
               if self.excinfo is None:
                   return f"<CallInfo when={self.when!r} result: {self._result!r}>"
               return f"<CallInfo when={self.when!r} excinfo={self.excinfo!r}>"
       
       
    1: def pytest_runtest_makereport(item: Item, call: CallInfo[None]) -> TestReport:
   18:     return TestReport.from_item_and_call(item, call)
       
       
    1: def pytest_make_collect_report(collector: Collector) -> CollectReport:
    5:     def collect() -> list[Item | Collector]:
               # Before collecting, if this is a Directory, load the conftests.
               # If a conftest import fails to load, it is considered a collection
               # error of the Directory collector. This is why it's done inside of the
               # CallInfo wrapper.
               #
               # Note: initial conftests are loaded early, not here.
    5:         if isinstance(collector, Directory):
    6:             collector.config.pluginmanager._loadconftestmodules(
    3:                 collector.path,
    3:                 collector.config.getoption("importmode"),
    3:                 rootpath=collector.config.rootpath,
    6:                 consider_namespace_packages=collector.config.getini(
    3:                     "consider_namespace_packages"
                       ),
                   )
       
    5:         return list(collector.collect())
       
   10:     call = CallInfo.from_call(
    5:         collect, "collect", reraise=(KeyboardInterrupt, SystemExit)
           )
    5:     longrepr: None | tuple[str, int, str] | str | TerminalRepr = None
    5:     if not call.excinfo:
    5:         outcome: Literal["passed", "skipped", "failed"] = "passed"
           else:
               skip_exceptions = [Skipped]
               unittest = sys.modules.get("unittest")
               if unittest is not None:
                   skip_exceptions.append(unittest.SkipTest)
               if isinstance(call.excinfo.value, tuple(skip_exceptions)):
                   outcome = "skipped"
                   r_ = collector._repr_failure_py(call.excinfo, "line")
                   assert isinstance(r_, ExceptionChainRepr), repr(r_)
                   r = r_.reprcrash
                   assert r
                   longrepr = (str(r.path), r.lineno, r.message)
               else:
                   outcome = "failed"
                   errorinfo = collector.repr_failure(call.excinfo)
                   if not hasattr(errorinfo, "toterminal"):
                       assert isinstance(errorinfo, str)
                       errorinfo = CollectErrorRepr(errorinfo)
                   longrepr = errorinfo
    5:     result = call.result if not call.excinfo else None
    5:     rep = CollectReport(collector.nodeid, outcome, longrepr, result)
    5:     rep.call = call  # type: ignore # see collect_one_node
    5:     return rep
       
       
    2: class SetupState:
    1:     """Shared state for setting up/tearing down test items or collectors
           in a session.
       
           Suppose we have a collection tree as follows:
       
           <Session session>
               <Module mod1>
                   <Function item1>
               <Module mod2>
                   <Function item2>
       
           The SetupState maintains a stack. The stack starts out empty:
       
               []
       
           During the setup phase of item1, setup(item1) is called. What it does
           is:
       
               push session to stack, run session.setup()
               push mod1 to stack, run mod1.setup()
               push item1 to stack, run item1.setup()
       
           The stack is:
       
               [session, mod1, item1]
       
           While the stack is in this shape, it is allowed to add finalizers to
           each of session, mod1, item1 using addfinalizer().
       
           During the teardown phase of item1, teardown_exact(item2) is called,
           where item2 is the next item to item1. What it does is:
       
               pop item1 from stack, run its teardowns
               pop mod1 from stack, run its teardowns
       
           mod1 was popped because it ended its purpose with item1. The stack is:
       
               [session]
       
           During the setup phase of item2, setup(item2) is called. What it does
           is:
       
               push mod2 to stack, run mod2.setup()
               push item2 to stack, run item2.setup()
       
           Stack:
       
               [session, mod2, item2]
       
           During the teardown phase of item2, teardown_exact(None) is called,
           because item2 is the last item. What it does is:
       
               pop item2 from stack, run its teardowns
               pop mod2 from stack, run its teardowns
               pop session from stack, run its teardowns
       
           Stack:
       
               []
       
           The end!
           """
       
    1:     def __init__(self) -> None:
               # The stack is in the dict insertion order.
    1:         self.stack: dict[
                   Node,
                   tuple[
                       # Node's finalizers.
                       list[Callable[[], object]],
                       # Node's exception and original traceback, if its setup raised.
                       tuple[OutcomeException | Exception, types.TracebackType | None] | None,
                   ],
    1:         ] = {}
       
    1:     def is_node_active(self, node: Node) -> bool:
               """Check if a node is currently active in the stack -- set up and not
               torn down yet."""
               return node in self.stack
       
    1:     def setup(self, item: Item) -> None:
               """Setup objects along the collector chain to the item."""
    6:         needed_collectors = item.listchain()
       
               # If a collector fails its setup, fail its entire subtree of items.
               # The setup is not retried for each item - the same exception is used.
   31:         for col, (finalizers, exc) in self.stack.items():
   25:             assert col in needed_collectors, "previous item was not torn down properly"
   25:             if exc:
                       raise exc[0].with_traceback(exc[1])
       
   17:         for col in needed_collectors[len(self.stack) :]:
   11:             assert col not in self.stack
                   # Push onto the stack.
   11:             self.stack[col] = ([col.teardown], None)
   11:             try:
   11:                 col.setup()
                   except TEST_OUTCOME as exc:
                       self.stack[col] = (self.stack[col][0], (exc, exc.__traceback__))
                       raise
       
    1:     def addfinalizer(self, finalizer: Callable[[], object], node: Node) -> None:
               """Attach a finalizer to the given node.
       
               The node must be currently active in the stack.
               """
               assert node and not isinstance(node, tuple)
               assert callable(finalizer)
               assert node in self.stack, (node, self.stack)
               self.stack[node][0].append(finalizer)
       
    1:     def teardown_exact(self, nextitem: Item | None) -> None:
               """Teardown the current stack up until reaching nodes that nextitem
               also descends from.
       
               When nextitem is None (meaning we're at the last item), the entire
               stack is torn down.
               """
    7:         needed_collectors = (nextitem and nextitem.listchain()) or []
    7:         exceptions: list[BaseException] = []
   18:         while self.stack:
   16:             if list(self.stack.keys()) == needed_collectors[: len(self.stack)]:
    5:                 break
   11:             node, (finalizers, _) = self.stack.popitem()
   11:             these_exceptions = []
   22:             while finalizers:
   11:                 fin = finalizers.pop()
   11:                 try:
   11:                     fin()
                       except TEST_OUTCOME as e:
                           these_exceptions.append(e)
       
   11:             if len(these_exceptions) == 1:
                       exceptions.extend(these_exceptions)
   11:             elif these_exceptions:
                       msg = f"errors while tearing down {node!r}"
                       exceptions.append(BaseExceptionGroup(msg, these_exceptions[::-1]))
       
    7:         if len(exceptions) == 1:
                   raise exceptions[0]
    7:         elif exceptions:
                   raise BaseExceptionGroup("errors during test teardown", exceptions[::-1])
    7:         if nextitem is None:
    2:             assert not self.stack
       
       
    1: def collect_one_node(collector: Collector) -> CollectReport:
    5:     ihook = collector.ihook
    5:     ihook.pytest_collectstart(collector=collector)
    5:     rep: CollectReport = ihook.pytest_make_collect_report(collector=collector)
    5:     call = rep.__dict__.pop("call", None)
    5:     if call and check_interactive_exception(call, rep):
               ihook.pytest_exception_interact(node=collector, call=call, report=rep)
    5:     return rep
