       # Copyright The OpenTelemetry Authors
       # SPDX-License-Identifier: Apache-2.0
       
       # pylint: disable=too-many-ancestors
       
       
    1: from abc import ABC, abstractmethod
    1: from collections.abc import Callable, Generator, Iterable, Sequence
    1: from dataclasses import dataclass
    1: from logging import getLogger
    1: from re import compile as re_compile
    1: from typing import (
           Generic,
           TypeVar,
       )
       
       # pylint: disable=unused-import; needed for typing and sphinx
    1: from opentelemetry import metrics
    1: from opentelemetry.context import Context
    1: from opentelemetry.metrics._internal.observation import Observation
    1: from opentelemetry.util.types import (
           Attributes,
       )
       
    1: _logger = getLogger(__name__)
       
    1: _name_regex = re_compile(r"[a-zA-Z][-_./a-zA-Z0-9]{0,254}")
    1: _unit_regex = re_compile(r"[\x00-\x7F]{0,63}")
       
       
    3: @dataclass(frozen=True)
    2: class _MetricsHistogramAdvisory:
    1:     explicit_bucket_boundaries: Sequence[float] | None = None
       
       
    3: @dataclass(frozen=True)
    2: class CallbackOptions:
    1:     """Options for the callback
       
           Args:
               timeout_millis: Timeout for the callback's execution. If the callback does asynchronous
                   work (e.g. HTTP requests), it should respect this timeout.
           """
       
    1:     timeout_millis: float = 10_000
       
       
    1: InstrumentT = TypeVar("InstrumentT", bound="Instrument")
       # pylint: disable=invalid-name
    1: CallbackT = (
    2:     Callable[[CallbackOptions], Iterable[Observation]]
    1:     | Generator[Iterable[Observation], CallbackOptions, None]
       )
       
       
    2: class Instrument(ABC):
    1:     """Abstract class that serves as base for all instruments."""
       
    2:     @abstractmethod
    6:     def __init__(
               self,
    1:         name: str,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               pass
       
    2:     @staticmethod
    6:     def _check_name_unit_description(
    3:         name: str, unit: str, description: str
    1:     ) -> dict[str, str | None]:
               """
               Checks the following instrument name, unit and description for
               compliance with the spec.
       
               Returns a dict with keys "name", "unit" and "description", the
               corresponding values will be the checked strings or `None` if the value
               is invalid. If valid, the checked strings should be used instead of the
               original values.
               """
       
               result: dict[str, str | None] = {}
       
               if _name_regex.fullmatch(name) is not None:
                   result["name"] = name
               else:
                   result["name"] = None
       
               if unit is None:
                   unit = ""
               if _unit_regex.fullmatch(unit) is not None:
                   result["unit"] = unit
               else:
                   result["unit"] = None
       
               if description is None:
                   result["description"] = ""
               else:
                   result["description"] = description
       
               return result
       
       
    2: class _ProxyInstrument(ABC, Generic[InstrumentT]):
    5:     def __init__(
               self,
    1:         name: str,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               self._name = name
               self._unit = unit
               self._description = description
               self._real_instrument: InstrumentT | None = None
       
    1:     def on_meter_set(self, meter: "metrics.Meter") -> None:
               """Called when a real meter is set on the creating _ProxyMeter"""
       
               # We don't need any locking on proxy instruments because it's OK if some
               # measurements get dropped while a real backing instrument is being
               # created.
               self._real_instrument = self._create_real_instrument(meter)
       
    2:     @abstractmethod
    2:     def _create_real_instrument(self, meter: "metrics.Meter") -> InstrumentT:
               """Create an instance of the real instrument. Implement this."""
       
       
    2: class _ProxyAsynchronousInstrument(_ProxyInstrument[InstrumentT]):
    6:     def __init__(
               self,
    1:         name: str,
    2:         callbacks: Sequence[CallbackT] | None = None,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               super().__init__(name, unit, description)
               self._callbacks = callbacks
       
       
    2: class Synchronous(Instrument):
    1:     """Base class for all synchronous instruments"""
       
       
    2: class Asynchronous(Instrument):
    1:     """Base class for all asynchronous instruments"""
       
    2:     @abstractmethod
    7:     def __init__(
               self,
    1:         name: str,
    2:         callbacks: Sequence[CallbackT] | None = None,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               super().__init__(name, unit=unit, description=description)
       
       
    2: class Counter(Synchronous):
    1:     """A Counter is a synchronous `Instrument` which supports non-negative increments."""
       
    2:     @abstractmethod
    6:     def add(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               """Records an increment to the counter.
       
               Args:
                   amount: The amount to increment the counter by. Must be non-negative.
                   attributes: Optional set of attributes to associate with the measurement.
                   context: Optional context to associate with the measurement. If not
                       provided, the current context is used.
               """
       
       
    2: class NoOpCounter(Counter):
    1:     """No-op implementation of `Counter`."""
       
    5:     def __init__(
               self,
    1:         name: str,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               super().__init__(name, unit=unit, description=description)
       
    5:     def add(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               return super().add(amount, attributes=attributes, context=context)
       
       
    2: class _ProxyCounter(_ProxyInstrument[Counter], Counter):
    5:     def add(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               if self._real_instrument:
                   self._real_instrument.add(amount, attributes, context)
       
    1:     def _create_real_instrument(self, meter: "metrics.Meter") -> Counter:
               return meter.create_counter(
                   self._name,
                   self._unit,
                   self._description,
               )
       
       
    2: class UpDownCounter(Synchronous):
    1:     """An UpDownCounter is a synchronous `Instrument` which supports increments and decrements."""
       
    2:     @abstractmethod
    6:     def add(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               """Records an increment or decrement to the counter.
       
               Unlike `Counter`, the ``amount`` may be negative, allowing the
               instrument to track values that go up and down (e.g. number of
               active requests, queue depth).
       
               Args:
                   amount: The amount to add to the counter. May be positive or negative.
                   attributes: Optional set of attributes to associate with the measurement.
                   context: Optional context to associate with the measurement. If not
                       provided, the current context is used.
               """
       
       
    2: class NoOpUpDownCounter(UpDownCounter):
    1:     """No-op implementation of `UpDownCounter`."""
       
    5:     def __init__(
               self,
    1:         name: str,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               super().__init__(name, unit=unit, description=description)
       
    5:     def add(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               return super().add(amount, attributes=attributes, context=context)
       
       
    2: class _ProxyUpDownCounter(_ProxyInstrument[UpDownCounter], UpDownCounter):
    5:     def add(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               if self._real_instrument:
                   self._real_instrument.add(amount, attributes, context)
       
    1:     def _create_real_instrument(self, meter: "metrics.Meter") -> UpDownCounter:
               return meter.create_up_down_counter(
                   self._name,
                   self._unit,
                   self._description,
               )
       
       
    2: class ObservableCounter(Asynchronous):
    1:     """An ObservableCounter is an asynchronous `Instrument` which reports monotonically
           increasing value(s) when the instrument is being observed.
           """
       
       
    2: class NoOpObservableCounter(ObservableCounter):
    1:     """No-op implementation of `ObservableCounter`."""
       
    6:     def __init__(
               self,
    1:         name: str,
    2:         callbacks: Sequence[CallbackT] | None = None,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               super().__init__(
                   name,
                   callbacks,
                   unit=unit,
                   description=description,
               )
       
       
    3: class _ProxyObservableCounter(
    1:     _ProxyAsynchronousInstrument[ObservableCounter], ObservableCounter
       ):
    3:     def _create_real_instrument(
    1:         self, meter: "metrics.Meter"
    1:     ) -> ObservableCounter:
               return meter.create_observable_counter(
                   self._name,
                   self._callbacks,
                   self._unit,
                   self._description,
               )
       
       
    2: class ObservableUpDownCounter(Asynchronous):
    1:     """An ObservableUpDownCounter is an asynchronous `Instrument` which reports additive value(s) (e.g.
           the process heap size - it makes sense to report the heap size from multiple processes and sum them
           up, so we get the total heap usage) when the instrument is being observed.
           """
       
       
    2: class NoOpObservableUpDownCounter(ObservableUpDownCounter):
    1:     """No-op implementation of `ObservableUpDownCounter`."""
       
    6:     def __init__(
               self,
    1:         name: str,
    2:         callbacks: Sequence[CallbackT] | None = None,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               super().__init__(
                   name,
                   callbacks,
                   unit=unit,
                   description=description,
               )
       
       
    3: class _ProxyObservableUpDownCounter(
    1:     _ProxyAsynchronousInstrument[ObservableUpDownCounter],
    1:     ObservableUpDownCounter,
       ):
    3:     def _create_real_instrument(
    1:         self, meter: "metrics.Meter"
    1:     ) -> ObservableUpDownCounter:
               return meter.create_observable_up_down_counter(
                   self._name,
                   self._callbacks,
                   self._unit,
                   self._description,
               )
       
       
    2: class Histogram(Synchronous):
    1:     """Histogram is a synchronous `Instrument` which can be used to report arbitrary values
           that are likely to be statistically meaningful. It is intended for statistics such as
           histograms, summaries, and percentile.
           """
       
    2:     @abstractmethod
    7:     def __init__(
               self,
    1:         name: str,
    2:         unit: str = "",
    2:         description: str = "",
    2:         explicit_bucket_boundaries_advisory: Sequence[float] | None = None,
    1:     ) -> None:
               pass
       
    2:     @abstractmethod
    6:     def record(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               """Records a measurement.
       
               Used to report measurements that are likely to be statistically
               meaningful, such as request durations, payload sizes, or any value
               for which a distribution (e.g. percentiles) is useful.
       
               Args:
                   amount: The measurement to record. Should be non-negative in most
                       cases; negative values are only meaningful when the histogram
                       is used to track signed deltas.
                   attributes: Optional set of attributes to associate with the measurement.
                   context: Optional context to associate with the measurement. If not
                       provided, the current context is used.
               """
       
       
    2: class NoOpHistogram(Histogram):
    1:     """No-op implementation of `Histogram`."""
       
    6:     def __init__(
               self,
    1:         name: str,
    2:         unit: str = "",
    2:         description: str = "",
    2:         explicit_bucket_boundaries_advisory: Sequence[float] | None = None,
    1:     ) -> None:
               super().__init__(
                   name,
                   unit=unit,
                   description=description,
                   explicit_bucket_boundaries_advisory=explicit_bucket_boundaries_advisory,
               )
       
    5:     def record(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               return super().record(amount, attributes=attributes, context=context)
       
       
    2: class _ProxyHistogram(_ProxyInstrument[Histogram], Histogram):
    6:     def __init__(
               self,
    1:         name: str,
    2:         unit: str = "",
    2:         description: str = "",
    2:         explicit_bucket_boundaries_advisory: Sequence[float] | None = None,
    1:     ) -> None:
               super().__init__(name, unit=unit, description=description)
               self._explicit_bucket_boundaries_advisory = (
                   explicit_bucket_boundaries_advisory
               )
       
    5:     def record(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               if self._real_instrument:
                   self._real_instrument.record(amount, attributes, context)
       
    1:     def _create_real_instrument(self, meter: "metrics.Meter") -> Histogram:
               return meter.create_histogram(
                   self._name,
                   self._unit,
                   self._description,
                   explicit_bucket_boundaries_advisory=self._explicit_bucket_boundaries_advisory,
               )
       
       
    2: class ObservableGauge(Asynchronous):
    1:     """Asynchronous Gauge is an asynchronous `Instrument` which reports non-additive value(s) (e.g.
           the room temperature - it makes no sense to report the temperature value from multiple rooms
           and sum them up) when the instrument is being observed.
           """
       
       
    2: class NoOpObservableGauge(ObservableGauge):
    1:     """No-op implementation of `ObservableGauge`."""
       
    6:     def __init__(
               self,
    1:         name: str,
    2:         callbacks: Sequence[CallbackT] | None = None,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               super().__init__(
                   name,
                   callbacks,
                   unit=unit,
                   description=description,
               )
       
       
    3: class _ProxyObservableGauge(
    1:     _ProxyAsynchronousInstrument[ObservableGauge],
    1:     ObservableGauge,
       ):
    3:     def _create_real_instrument(
    1:         self, meter: "metrics.Meter"
    1:     ) -> ObservableGauge:
               return meter.create_observable_gauge(
                   self._name,
                   self._callbacks,
                   self._unit,
                   self._description,
               )
       
       
    2: class Gauge(Synchronous):
    1:     """A Gauge is a synchronous `Instrument` which can be used to record non-additive values as they occur."""
       
    2:     @abstractmethod
    6:     def set(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               """Records the current value of the gauge.
       
               The gauge reports the last recorded value when observed. It is
               intended for non-additive measurements where only the current
               value matters (e.g. CPU utilisation percentage, room temperature).
       
               Args:
                   amount: The current value to record.
                   attributes: Optional set of attributes to associate with the measurement.
                   context: Optional context to associate with the measurement. If not
                       provided, the current context is used.
               """
       
       
    2: class NoOpGauge(Gauge):
    1:     """No-op implementation of ``Gauge``."""
       
    5:     def __init__(
               self,
    1:         name: str,
    2:         unit: str = "",
    2:         description: str = "",
    1:     ) -> None:
               super().__init__(name, unit=unit, description=description)
       
    5:     def set(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               return super().set(amount, attributes=attributes, context=context)
       
       
    3: class _ProxyGauge(
    1:     _ProxyInstrument[Gauge],
    1:     Gauge,
       ):
    5:     def set(
               self,
    1:         amount: int | float,
    2:         attributes: Attributes | None = None,
    2:         context: Context | None = None,
    1:     ) -> None:
               if self._real_instrument:
                   self._real_instrument.set(amount, attributes, context)
       
    1:     def _create_real_instrument(self, meter: "metrics.Meter") -> Gauge:
               return meter.create_gauge(
                   self._name,
                   self._unit,
                   self._description,
               )
