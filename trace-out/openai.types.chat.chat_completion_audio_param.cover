       # File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
       
    1: from __future__ import annotations
       
    1: from typing import Union
    1: from typing_extensions import Literal, Required, TypeAlias, TypedDict
       
    1: __all__ = ["ChatCompletionAudioParam", "Voice", "VoiceID"]
       
       
    2: class VoiceID(TypedDict, total=False):
    1:     """Custom voice reference."""
       
    1:     id: Required[str]
    1:     """The custom voice ID, e.g. `voice_1234`."""
       
       
    2: Voice: TypeAlias = Union[
    1:     str, Literal["alloy", "ash", "ballad", "coral", "echo", "sage", "shimmer", "verse", "marin", "cedar"], VoiceID
       ]
       
       
    2: class ChatCompletionAudioParam(TypedDict, total=False):
    1:     """Parameters for audio output.
       
           Required when audio output is requested with
           `modalities: ["audio"]`. [Learn more](https://platform.openai.com/docs/guides/audio).
           """
       
    1:     format: Required[Literal["wav", "aac", "mp3", "flac", "opus", "pcm16"]]
    1:     """Specifies the output audio format.
       
           Must be one of `wav`, `mp3`, `flac`, `opus`, or `pcm16`.
           """
       
    1:     voice: Required[Voice]
    1:     """The voice the model uses to respond.
       
           Supported built-in voices are `alloy`, `ash`, `ballad`, `coral`, `echo`,
           `fable`, `nova`, `onyx`, `sage`, `shimmer`, `marin`, and `cedar`. You may also
           provide a custom voice object with an `id`, for example
           `{ "id": "voice_1234" }`.
           """
