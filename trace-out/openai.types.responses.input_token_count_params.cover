       # File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
       
    1: from __future__ import annotations
       
    1: from typing import Union, Iterable, Optional
    1: from typing_extensions import Literal, TypeAlias, TypedDict
       
    1: from .tool_param import ToolParam
    1: from .tool_choice_options import ToolChoiceOptions
    1: from .tool_choice_mcp_param import ToolChoiceMcpParam
    1: from .tool_choice_shell_param import ToolChoiceShellParam
    1: from .tool_choice_types_param import ToolChoiceTypesParam
    1: from ..shared_params.reasoning import Reasoning
    1: from .tool_choice_custom_param import ToolChoiceCustomParam
    1: from .response_input_item_param import ResponseInputItemParam
    1: from .tool_choice_allowed_param import ToolChoiceAllowedParam
    1: from .tool_choice_function_param import ToolChoiceFunctionParam
    1: from .tool_choice_apply_patch_param import ToolChoiceApplyPatchParam
    1: from .response_conversation_param_param import ResponseConversationParamParam
    1: from .response_format_text_config_param import ResponseFormatTextConfigParam
       
    1: __all__ = ["InputTokenCountParams", "Conversation", "Text", "ToolChoice"]
       
       
    2: class InputTokenCountParams(TypedDict, total=False):
    1:     conversation: Optional[Conversation]
    1:     """The conversation that this response belongs to.
       
           Items from this conversation are prepended to `input_items` for this response
           request. Input items and output items from this response are automatically added
           to this conversation after this response completes.
           """
       
    1:     input: Union[str, Iterable[ResponseInputItemParam], None]
    1:     """Text, image, or file inputs to the model, used to generate a response"""
       
    1:     instructions: Optional[str]
    1:     """
           A system (or developer) message inserted into the model's context. When used
           along with `previous_response_id`, the instructions from a previous response
           will not be carried over to the next response. This makes it simple to swap out
           system (or developer) messages in new responses.
           """
       
    1:     model: Optional[str]
    1:     """Model ID used to generate the response, like `gpt-4o` or `o3`.
       
           OpenAI offers a wide range of models with different capabilities, performance
           characteristics, and price points. Refer to the
           [model guide](https://platform.openai.com/docs/models) to browse and compare
           available models.
           """
       
    1:     parallel_tool_calls: Optional[bool]
    1:     """Whether to allow the model to run tool calls in parallel."""
       
    1:     personality: Union[str, Literal["friendly", "pragmatic"]]
    1:     """A model-owned style preset to apply to this request.
       
           Omit this parameter to use the model's default style. Supported values may
           expand over time. Values must be at most 64 characters.
           """
       
    1:     previous_response_id: Optional[str]
    1:     """The unique ID of the previous response to the model.
       
           Use this to create multi-turn conversations. Learn more about
           [conversation state](https://platform.openai.com/docs/guides/conversation-state).
           Cannot be used in conjunction with `conversation`.
           """
       
    1:     reasoning: Optional[Reasoning]
    1:     """
           **gpt-5 and o-series models only** Configuration options for
           [reasoning models](https://platform.openai.com/docs/guides/reasoning).
           """
       
    1:     text: Optional[Text]
    1:     """Configuration options for a text response from the model.
       
           Can be plain text or structured JSON data. Learn more:
       
           - [Text inputs and outputs](https://platform.openai.com/docs/guides/text)
           - [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs)
           """
       
    1:     tool_choice: Optional[ToolChoice]
    1:     """Controls which tool the model should use, if any."""
       
    1:     tools: Optional[Iterable[ToolParam]]
    1:     """An array of tools the model may call while generating a response.
       
           You can specify which tool to use by setting the `tool_choice` parameter.
           """
       
    1:     truncation: Literal["auto", "disabled"]
    1:     """The truncation strategy to use for the model response.
       
           - `auto`: If the input to this Response exceeds the model's context window size,
             the model will truncate the response to fit the context window by dropping
             items from the beginning of the conversation. - `disabled` (default): If the
             input size will exceed the context window size for a model, the request will
             fail with a 400 error.
           """
       
       
    1: Conversation: TypeAlias = Union[str, ResponseConversationParamParam]
       
       
    2: class Text(TypedDict, total=False):
    1:     """Configuration options for a text response from the model.
       
           Can be plain
           text or structured JSON data. Learn more:
           - [Text inputs and outputs](https://platform.openai.com/docs/guides/text)
           - [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs)
           """
       
    1:     format: ResponseFormatTextConfigParam
    1:     """An object specifying the format that the model must output.
       
           Configuring `{ "type": "json_schema" }` enables Structured Outputs, which
           ensures the model will match your supplied JSON schema. Learn more in the
           [Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs).
       
           The default format is `{ "type": "text" }` with no additional options.
       
           **Not recommended for gpt-4o and newer models:**
       
           Setting to `{ "type": "json_object" }` enables the older JSON mode, which
           ensures the message the model generates is valid JSON. Using `json_schema` is
           preferred for models that support it.
           """
       
    1:     verbosity: Optional[Literal["low", "medium", "high"]]
    1:     """Constrains the verbosity of the model's response.
       
           Lower values will result in more concise responses, while higher values will
           result in more verbose responses. Currently supported values are `low`,
           `medium`, and `high`.
           """
       
       
    2: ToolChoice: TypeAlias = Union[
    2:     ToolChoiceOptions,
    1:     ToolChoiceAllowedParam,
    1:     ToolChoiceTypesParam,
    1:     ToolChoiceFunctionParam,
    1:     ToolChoiceMcpParam,
    1:     ToolChoiceCustomParam,
    1:     ToolChoiceApplyPatchParam,
    1:     ToolChoiceShellParam,
       ]
