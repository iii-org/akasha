       # mypy: allow-untyped-defs
    1: from typing import Any, Literal, Optional
       
    1: import torch
    1: import torch.nn as nn
    1: import torch.nn.functional as F
    1: from torch.nn.common_types import _size_1_t
       
    1: from .utils import ReferenceQuantizedModule
       
       
    1: __all__ = [
           "Conv1d",
           "Conv2d",
           "Conv3d",
           "ConvTranspose1d",
           "ConvTranspose2d",
           "ConvTranspose3d",
       ]
       
       
    2: class _ConvNd(torch.nn.modules.conv._ConvNd, ReferenceQuantizedModule):
    1:     """A reference version of nn.quantized.Conv2d
           we will not pack the parameters in this module, since weight packing is an
           optimization for quantized backends supported in PyTorch (fbgemm/qnnpack),
           this is useful when user want to use this module in other backends like Glow.
           """
       
    1:     __annotations__ = {"bias": Optional[torch.Tensor]}
    1:     _IS_REFERENCE = True
       
    2:     @staticmethod
    2:     def from_float(cls, float_conv, weight_qparams):
               qref_conv = cls(
                   float_conv.in_channels,
                   float_conv.out_channels,
                   float_conv.kernel_size,  # type: ignore[arg-type]
                   float_conv.stride,  # type: ignore[arg-type]
                   float_conv.padding,  # type: ignore[arg-type]
                   float_conv.dilation,  # type: ignore[arg-type]
                   float_conv.groups,
                   float_conv.bias is not None,  # type: ignore[arg-type]
                   float_conv.padding_mode,
                   device=float_conv.weight.device,
                   dtype=float_conv.weight.dtype,
                   weight_qparams=weight_qparams,
               )
               qref_conv.weight = torch.nn.Parameter(float_conv.weight.detach())
               if float_conv.bias is not None:
                   qref_conv.bias = torch.nn.Parameter(float_conv.bias.detach())
               return qref_conv
       
       
    2: class Conv1d(_ConvNd, nn.Conv1d):
   11:     def __init__(
               self,
    1:         in_channels: int,
    1:         out_channels: int,
    1:         kernel_size: _size_1_t,
    2:         stride: _size_1_t = 1,
    2:         padding: _size_1_t = 0,
    2:         dilation: _size_1_t = 1,
    2:         groups: int = 1,
    2:         bias: bool = True,
    2:         padding_mode: Literal["zeros", "reflect", "replicate", "circular"] = "zeros",
    1:         device=None,
    1:         dtype=None,
    2:         weight_qparams: dict[str, Any] | None = None,
           ):
               nn.Conv1d.__init__(
                   self,
                   in_channels,
                   out_channels,
                   kernel_size,
                   stride,
                   padding,
                   dilation,
                   groups,
                   bias,
                   padding_mode,
                   device,
                   dtype,
               )
               self._init_weight_qparams(weight_qparams, device)
       
    1:     def forward(self, x: torch.Tensor) -> torch.Tensor:
               """
               we have:
               w(float) -- quant - dequant \
               x(float) ------------- F.conv1d ---
       
               In the full model, we will see
               w(float) -- quant - *dequant \
               x -- quant --- *dequant --  *F.conv1d --- *quant - dequant
               and the backend should be able to fuse the ops with `*` into a quantized conv1d
               """
               weight_quant_dequant = self.get_weight()
       
               result = F.conv1d(
                   x,
                   weight_quant_dequant,
                   self.bias,
                   self.stride,
                   self.padding,
                   self.dilation,
                   self.groups,
               )
               return result
       
    1:     def _get_name(self):
               return "QuantizedConv1d(Reference)"
       
    2:     @classmethod
    2:     def from_float(cls, float_conv, weight_qparams):  # type: ignore[override]
               return _ConvNd.from_float(cls, float_conv, weight_qparams)
       
       
    2: class Conv2d(_ConvNd, nn.Conv2d):
    2:     def __init__(
               self,
               in_channels,
               out_channels,
               kernel_size,
    1:         stride=1,
    1:         padding=0,
    1:         dilation=1,
    1:         groups=1,
    1:         bias=True,
    1:         padding_mode="zeros",
    1:         device=None,
    1:         dtype=None,
    2:         weight_qparams: dict[str, Any] | None = None,
           ):
               nn.Conv2d.__init__(
                   self,
                   in_channels,
                   out_channels,
                   kernel_size,
                   stride,
                   padding,
                   dilation,
                   groups,
                   bias,
                   # pyrefly: ignore [bad-argument-type]
                   padding_mode,
                   device,
                   dtype,
               )
               self._init_weight_qparams(weight_qparams, device)
       
    1:     def forward(self, x: torch.Tensor) -> torch.Tensor:
               """
               we have:
               w(float) -- quant - dequant \
               x(float) ------------- F.conv2d ---
       
               In the full model, we will see
               w(float) -- quant - *dequant \
               x -- quant --- *dequant --  *F.conv2d --- *quant - dequant
               and the backend should be able to fuse the ops with `*` into a quantized conv2d
               """
               weight_quant_dequant = self.get_weight()
       
               result = F.conv2d(
                   x,
                   weight_quant_dequant,
                   self.bias,
                   self.stride,
                   self.padding,
                   self.dilation,
                   self.groups,
               )
               return result
       
    1:     def _get_name(self):
               return "QuantizedConv2d(Reference)"
       
    2:     @classmethod
    2:     def from_float(cls, float_conv, weight_qparams):  # type: ignore[override]
               return _ConvNd.from_float(cls, float_conv, weight_qparams)
       
       
    2: class Conv3d(_ConvNd, nn.Conv3d):
    2:     def __init__(
               self,
               in_channels,
               out_channels,
               kernel_size,
    1:         stride=1,
    1:         padding=0,
    1:         dilation=1,
    1:         groups=1,
    1:         bias=True,
    1:         padding_mode="zeros",
    1:         device=None,
    1:         dtype=None,
    2:         weight_qparams: dict[str, Any] | None = None,
           ):
               nn.Conv3d.__init__(
                   self,
                   in_channels,
                   out_channels,
                   kernel_size,
                   stride,
                   padding,
                   dilation,
                   groups,
                   bias,
                   # pyrefly: ignore [bad-argument-type]
                   padding_mode,
                   device,
                   dtype,
               )
               self._init_weight_qparams(weight_qparams, device)
       
    1:     def forward(self, x: torch.Tensor) -> torch.Tensor:
               """
               we have:
               w(float) -- quant - dequant \
               x(float) ------------- F.conv3d ---
       
               In the full model, we will see
               w(float) -- quant - *dequant \
               x -- quant --- *dequant --  *F.conv3d --- *quant - dequant
               and the backend should be able to fuse the ops with `*` into a quantized conv3d
               """
               weight_quant_dequant = self.get_weight()
       
               result = F.conv3d(
                   x,
                   weight_quant_dequant,
                   self.bias,
                   self.stride,
                   self.padding,
                   self.dilation,
                   self.groups,
               )
               return result
       
    1:     def _get_name(self):
               return "QuantizedConv3d(Reference)"
       
    2:     @classmethod
    2:     def from_float(cls, float_conv, weight_qparams):  # type: ignore[override]
               return _ConvNd.from_float(cls, float_conv, weight_qparams)
       
       
    2: class _ConvTransposeNd(_ConvNd, torch.nn.modules.conv._ConvTransposeNd):
    1:     """A reference version of nn.quantized.ConvTranspose2d
           we will not pack the parameters in this module, since weight packing is an
           optimization for quantized backends supported in PyTorch (fbgemm/qnnpack),
           this is useful when user want to use this module in other backends like Glow.
           """
       
    2:     @staticmethod
    2:     def from_float(cls, float_conv, weight_qparams):
               qref_conv = cls(
                   float_conv.in_channels,
                   float_conv.out_channels,
                   float_conv.kernel_size,  # type: ignore[arg-type]
                   float_conv.stride,  # type: ignore[arg-type]
                   float_conv.padding,  # type: ignore[arg-type]
                   float_conv.output_padding,  # type: ignore[arg-type]
                   float_conv.groups,
                   float_conv.bias is not None,  # type: ignore[arg-type]
                   float_conv.dilation,  # type: ignore[arg-type]
                   float_conv.padding_mode,
                   device=float_conv.weight.device,
                   dtype=float_conv.weight.dtype,
                   weight_qparams=weight_qparams,
               )
               qref_conv.weight = torch.nn.Parameter(float_conv.weight.detach())
               if float_conv.bias is not None:
                   qref_conv.bias = torch.nn.Parameter(float_conv.bias.detach())
               return qref_conv
       
       
    2: class ConvTranspose1d(_ConvTransposeNd, nn.ConvTranspose1d):
   12:     def __init__(
               self,
    1:         in_channels: int,
    1:         out_channels: int,
    1:         kernel_size: _size_1_t,
    2:         stride: _size_1_t = 1,
    2:         padding: _size_1_t = 0,
    2:         output_padding: _size_1_t = 0,
    2:         groups: int = 1,
    2:         bias: bool = True,
    2:         dilation: _size_1_t = 1,
    2:         padding_mode: Literal["zeros", "reflect", "replicate", "circular"] = "zeros",
    1:         device=None,
    1:         dtype=None,
    2:         weight_qparams: dict[str, Any] | None = None,
           ):
               nn.ConvTranspose1d.__init__(
                   self,
                   in_channels,
                   out_channels,
                   kernel_size,
                   stride,
                   padding,
                   output_padding,
                   groups,
                   bias,
                   dilation,
                   padding_mode,
                   device,
                   dtype,
               )
               self._init_weight_qparams(weight_qparams, device)
       
    4:     def forward(
    3:         self, x: torch.Tensor, output_size: list[int] | None = None
    1:     ) -> torch.Tensor:
               """
               we have:
               w(float) -- quant - dequant \
               x(float) ------------- F.convTranspose1d ---
               In the full model, we will see
               w(float) -- quant - *dequant \
               x -- quant --- *dequant --  *F.convTranspose1d --- *quant - dequant
               and the backend should be able to fuse the ops with `*` into a quantized conv1d
               """
       
               assert isinstance(self.padding, tuple)
               # One cannot replace List by Tuple or Sequence in "_output_padding" because
               # TorchScript does not support `Sequence[T]` or `Tuple[T, ...]`.
               output_padding = self._output_padding(
                   input,  # type: ignore[arg-type]
                   output_size,
                   self.stride,  # type: ignore[arg-type]
                   self.padding,  # type: ignore[arg-type]
                   self.kernel_size,  # type: ignore[arg-type]
                   self.dilation,  # type: ignore[arg-type]
               )
       
               weight_quant_dequant = self.get_weight()
               result = F.conv_transpose1d(
                   x,
                   weight_quant_dequant,
                   self.bias,
                   self.stride,
                   self.padding,
                   output_padding,
                   self.groups,
                   self.dilation,
               )
               return result
       
    1:     def _get_name(self):
               return "QuantizedConvTranspose1d(Reference)"
       
    2:     @classmethod
    2:     def from_float(cls, float_conv, weight_qparams):  # type: ignore[override]
               return _ConvTransposeNd.from_float(cls, float_conv, weight_qparams)
       
       
    2: class ConvTranspose2d(_ConvTransposeNd, nn.ConvTranspose2d):
    2:     def __init__(
               self,
               in_channels,
               out_channels,
               kernel_size,
    1:         stride=1,
    1:         padding=0,
    1:         output_padding=0,
    1:         groups=1,
    1:         bias=True,
    1:         dilation=1,
    1:         padding_mode="zeros",
    1:         device=None,
    1:         dtype=None,
    2:         weight_qparams: dict[str, Any] | None = None,
           ):
               nn.ConvTranspose2d.__init__(
                   self,
                   in_channels,
                   out_channels,
                   kernel_size,
                   stride,
                   padding,
                   output_padding,
                   groups,
                   bias,
                   dilation,
                   # pyrefly: ignore [bad-argument-type]
                   padding_mode,
                   device,
                   dtype,
               )
               self._init_weight_qparams(weight_qparams, device)
       
    4:     def forward(
    3:         self, x: torch.Tensor, output_size: list[int] | None = None
    1:     ) -> torch.Tensor:
               """
               we have:
               w(float) -- quant - dequant \
               x(float) ------------- F.convTranspose2d ---
               In the full model, we will see
               w(float) -- quant - *dequant \
               x -- quant --- *dequant --  *F.convTranspose2d --- *quant - dequant
               and the backend should be able to fuse the ops with `*` into a quantized conv2d
               """
               assert isinstance(self.padding, tuple)
               # One cannot replace List by Tuple or Sequence in "_output_padding" because
               # TorchScript does not support `Sequence[T]` or `Tuple[T, ...]`.
       
               output_padding = self._output_padding(
                   input,  # type: ignore[arg-type]
                   output_size,
                   self.stride,  # type: ignore[arg-type]
                   self.padding,  # type: ignore[arg-type]
                   self.kernel_size,  # type: ignore[arg-type]
                   self.dilation,  # type: ignore[arg-type]
               )
       
               weight_quant_dequant = self.get_weight()
               result = F.conv_transpose2d(
                   x,
                   weight_quant_dequant,
                   self.bias,
                   self.stride,
                   self.padding,
                   output_padding,
                   self.groups,
                   self.dilation,
               )
       
               return result
       
    1:     def _get_name(self):
               return "QuantizedConvTranspose2d(Reference)"
       
    2:     @classmethod
    2:     def from_float(cls, float_conv, weight_qparams):  # type: ignore[override]
               return _ConvTransposeNd.from_float(cls, float_conv, weight_qparams)
       
       
    2: class ConvTranspose3d(_ConvTransposeNd, nn.ConvTranspose3d):
    2:     def __init__(
               self,
               in_channels,
               out_channels,
               kernel_size,
    1:         stride=1,
    1:         padding=0,
    1:         output_padding=0,
    1:         groups=1,
    1:         bias=True,
    1:         dilation=1,
    1:         padding_mode="zeros",
    1:         device=None,
    1:         dtype=None,
    2:         weight_qparams: dict[str, Any] | None = None,
           ):
               nn.ConvTranspose3d.__init__(
                   self,
                   in_channels,
                   out_channels,
                   kernel_size,
                   stride,
                   padding,
                   output_padding,
                   groups,
                   bias,
                   dilation,
                   # pyrefly: ignore [bad-argument-type]
                   padding_mode,
                   device,
                   dtype,
               )
               self._init_weight_qparams(weight_qparams, device)
       
    4:     def forward(
    3:         self, x: torch.Tensor, output_size: list[int] | None = None
    1:     ) -> torch.Tensor:
               """
               we have:
               w(float) -- quant - dequant \
               x(float) ------------- F.convTranspose3d ---
               In the full model, we will see
               w(float) -- quant - *dequant \
               x -- quant --- *dequant --  *F.convTranspose3d --- *quant - dequant
               and the backend should be able to fuse the ops with `*` into a quantized conv3d
               """
       
               assert isinstance(self.padding, tuple)
               # One cannot replace List by Tuple or Sequence in "_output_padding" because
               # TorchScript does not support `Sequence[T]` or `Tuple[T, ...]`.
               output_padding = self._output_padding(
                   input,  # type: ignore[arg-type]
                   output_size,
                   self.stride,  # type: ignore[arg-type]
                   self.padding,  # type: ignore[arg-type]
                   self.kernel_size,  # type: ignore[arg-type]
                   self.dilation,  # type: ignore[arg-type]
               )
       
               weight_quant_dequant = self.get_weight()
               result = F.conv_transpose3d(
                   x,
                   weight_quant_dequant,
                   self.bias,
                   self.stride,
                   self.padding,
                   output_padding,
                   self.groups,
                   self.dilation,
               )
               return result
       
    1:     def _get_name(self):
               return "QuantizedConvTranspose3d(Reference)"
       
    2:     @classmethod
    2:     def from_float(cls, float_conv, weight_qparams):  # type: ignore[override]
               return _ConvTransposeNd.from_float(cls, float_conv, weight_qparams)
