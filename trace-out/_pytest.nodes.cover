       # mypy: allow-untyped-defs
    1: from __future__ import annotations
       
    1: import abc
    1: from collections.abc import Callable
    1: from collections.abc import Iterable
    1: from collections.abc import Iterator
    1: from collections.abc import MutableMapping
    1: from functools import cached_property
    1: from functools import lru_cache
    1: import os
    1: import pathlib
    1: from pathlib import Path
    1: from typing import Any
    1: from typing import cast
    1: from typing import NoReturn
    1: from typing import overload
    1: from typing import TYPE_CHECKING
    1: from typing import TypeVar
    1: import warnings
       
    1: import pluggy
       
    1: import _pytest._code
    1: from _pytest._code import getfslineno
    1: from _pytest._code.code import ExceptionInfo
    1: from _pytest._code.code import TerminalRepr
    1: from _pytest._code.code import Traceback
    1: from _pytest._code.code import TracebackStyle
    1: from _pytest.compat import LEGACY_PATH
    1: from _pytest.compat import signature
    1: from _pytest.config import Config
    1: from _pytest.config import ConftestImportFailure
    1: from _pytest.mark.structures import Mark
    1: from _pytest.mark.structures import MarkDecorator
    1: from _pytest.mark.structures import NodeKeywords
    1: from _pytest.outcomes import fail
    1: from _pytest.pathlib import absolutepath
    1: from _pytest.stash import Stash
    1: from _pytest.warning_types import PytestWarning
       
       
    1: if TYPE_CHECKING:
           from typing_extensions import Self
       
           # Imported here due to circular import.
           from _pytest.main import Session
       
       
    1: SEP = "/"
       
       
    1: def norm_sep(path: str | os.PathLike[str]) -> str:
           """Normalize path separators to forward slashes for nodeid compatibility.
       
           Replaces backslashes with forward slashes. This handles both Windows native
           paths and cross-platform data (e.g., Windows paths in serialized test reports
           when running on Linux).
       
           :param path: A path string or PathLike object.
           :returns: String with all backslashes replaced by forward slashes.
           """
   63:     return os.fspath(path).replace("\\", SEP)
       
       
    1: tracebackcutdir = Path(_pytest.__file__).parent
       
       
    1: _T = TypeVar("_T")
       
       
    1: _NodeType = TypeVar("_NodeType", bound="Node")
       
       
    2: class NodeMeta(abc.ABCMeta):
    1:     """Metaclass used by :class:`Node` to enforce that direct construction raises
           :class:`Failed`.
       
           This behaviour supports the indirection introduced with :meth:`Node.from_parent`,
           the named constructor to be used instead of direct construction. The design
           decision to enforce indirection with :class:`NodeMeta` was made as a
           temporary aid for refactoring the collection tree, which was diagnosed to
           have :class:`Node` objects whose creational patterns were overly entangled.
           Once the refactoring is complete, this metaclass can be removed.
       
           See https://github.com/pytest-dev/pytest/projects/3 for an overview of the
           progress on detangling the :class:`Node` classes.
           """
       
    1:     def __call__(cls, *k, **kw) -> NoReturn:
               msg = (
                   "Direct construction of {name} has been deprecated, please use {name}.from_parent.\n"
                   "See "
                   "https://docs.pytest.org/en/stable/deprecations.html#node-construction-changed-to-node-from-parent"
                   " for more details."
               ).format(name=f"{cls.__module__}.{cls.__name__}")
               fail(msg, pytrace=False)
       
    1:     def _create(cls: type[_T], *k, **kw) -> _T:
   45:         try:
   45:             return super().__call__(*k, **kw)  # type: ignore[no-any-return,misc]
               except TypeError:
                   sig = signature(getattr(cls, "__init__"))
                   known_kw = {k: v for k, v in kw.items() if k in sig.parameters}
                   from .warning_types import PytestDeprecationWarning
       
                   warnings.warn(
                       PytestDeprecationWarning(
                           f"{cls} is not using a cooperative constructor and only takes {set(known_kw)}.\n"
                           "See https://docs.pytest.org/en/stable/deprecations.html"
                           "#constructors-of-custom-pytest-node-subclasses-should-take-kwargs "
                           "for more details."
                       )
                   )
       
                   return super().__call__(*k, **known_kw)  # type: ignore[no-any-return,misc]
       
       
    2: class Node(abc.ABC, metaclass=NodeMeta):
    1:     r"""Base class of :class:`Collector` and :class:`Item`, the components of
           the test collection tree.
       
           ``Collector``\'s are the internal nodes of the tree, and ``Item``\'s are the
           leaf nodes.
           """
       
           # Implemented in the legacypath plugin.
           #: A ``LEGACY_PATH`` copy of the :attr:`path` attribute. Intended for usage
           #: for methods not migrated to ``pathlib.Path`` yet, such as
           #: :meth:`Item.reportinfo <pytest.Item.reportinfo>`. Will be deprecated in
           #: a future release, prefer using :attr:`path` instead.
    1:     fspath: LEGACY_PATH
       
           # Use __slots__ to make attribute access faster.
           # Note that __dict__ is still available.
    1:     __slots__ = (
               "__dict__",
               "_nodeid",
               "_store",
               "config",
               "name",
               "parent",
               "path",
               "session",
           )
       
    1:     def __init__(
               self,
               name: str,
    1:         parent: Node | None = None,
    1:         config: Config | None = None,
    1:         session: Session | None = None,
    1:         fspath: None = None,
    1:         path: Path | None = None,
    1:         nodeid: str | None = None,
           ) -> None:
               #: A unique name within the scope of the parent node.
   45:         self.name: str = name
       
               #: The parent collector node.
   45:         self.parent = parent
       
   45:         if config:
                   #: The pytest config object.
    1:             self.config: Config = config
               else:
   44:             if not parent:
                       raise TypeError("config or parent must be provided")
   44:             self.config = parent.config
       
   45:         if session:
                   #: The pytest session this node is part of.
   33:             self.session: Session = session
               else:
   12:             if not parent:
                       raise TypeError("session or parent must be provided")
   12:             self.session = parent.session
       
   45:         if path is None:
   12:             assert parent is not None
   12:             path = parent.path
               #: Filesystem path where this node was collected from.
   45:         self.path: pathlib.Path = path
       
               # The explicit annotation is to avoid publicly exposing NodeKeywords.
               #: Keywords/markers collected from all scopes.
   45:         self.keywords: MutableMapping[str, Any] = NodeKeywords(self)
       
               #: The marker objects belonging to this node.
   45:         self.own_markers: list[Mark] = []
       
               #: Allow adding of extra keywords to use for matching.
   45:         self.extra_keyword_matches: set[str] = set()
       
   45:         if nodeid is not None:
   33:             assert "::()" not in nodeid
   33:             self._nodeid = nodeid
               else:
   12:             if not self.parent:
                       raise TypeError("nodeid or parent must be provided")
   12:             self._nodeid = self.parent.nodeid + "::" + self.name
       
               #: A place where plugins can store information on the node for their
               #: own use.
   45:         self.stash: Stash = Stash()
               # Deprecated alias. Was never public. Can be removed in a few releases.
   45:         self._store = self.stash
       
    2:     @classmethod
    2:     def from_parent(cls, parent: Node, **kw) -> Self:
               """Public constructor for Nodes.
       
               This indirection got introduced in order to enable removing
               the fragile logic from the node constructors.
       
               Subclasses can use ``super().from_parent(...)`` when overriding the
               construction.
       
               :param parent: The parent node of this Node.
               """
   44:         if "config" in kw:
                   raise TypeError("config is not a valid argument for from_parent")
   44:         if "session" in kw:
                   raise TypeError("session is not a valid argument for from_parent")
   44:         return cls._create(parent=parent, **kw)
       
    2:     @property
    2:     def ihook(self) -> pluggy.HookRelay:
               """Path-sensitive hook proxy used to call pytest hooks."""
   65:         return self.session.gethookproxy(self.path)
       
    1:     def __repr__(self) -> str:
               return "<{} {}>".format(self.__class__.__name__, getattr(self, "name", None))
       
    1:     def warn(self, warning: Warning) -> None:
               """Issue a warning for this Node.
       
               Warnings will be displayed after the test session, unless explicitly suppressed.
       
               :param Warning warning:
                   The warning instance to issue.
       
               :raises ValueError: If ``warning`` instance is not a subclass of Warning.
       
               Example usage:
       
               .. code-block:: python
       
                   node.warn(PytestWarning("some message"))
                   node.warn(UserWarning("some message"))
       
               .. versionchanged:: 6.2
                   Any subclass of :class:`Warning` is now accepted, rather than only
                   :class:`PytestWarning <pytest.PytestWarning>` subclasses.
               """
               # enforce type checks here to avoid getting a generic type error later otherwise.
               if not isinstance(warning, Warning):
                   raise ValueError(
                       f"warning must be an instance of Warning or subclass, got {warning!r}"
                   )
               path, lineno = get_fslocation_from_item(self)
               assert lineno is not None
               warnings.warn_explicit(
                   warning,
                   category=None,
                   filename=str(path),
                   lineno=lineno + 1,
               )
       
           # Methods for ordering nodes.
       
    2:     @property
    2:     def nodeid(self) -> str:
               """A ::-separated string denoting its collection tree address."""
  138:         return self._nodeid
       
    1:     def __hash__(self) -> int:
  169:         return hash(self._nodeid)
       
    1:     def setup(self) -> None:
    5:         pass
       
    1:     def teardown(self) -> None:
   11:         pass
       
    1:     def iter_parents(self) -> Iterator[Node]:
               """Iterate over all parent collectors starting from and including self
               up to the root of the collection tree.
       
               .. versionadded:: 8.1
               """
   78:         parent: Node | None = self
  474:         while parent is not None:
  408:             yield parent
  396:             parent = parent.parent
       
    1:     def listchain(self) -> list[Node]:
               """Return a list of all parent collectors starting from the root of the
               collection tree down to and including self."""
   17:         chain = []
   17:         item: Node | None = self
  119:         while item is not None:
  102:             chain.append(item)
  102:             item = item.parent
   17:         chain.reverse()
   17:         return chain
       
    1:     def add_marker(self, marker: str | MarkDecorator, append: bool = True) -> None:
               """Dynamically add a marker object to the node.
       
               :param marker:
                   The marker.
               :param append:
                   Whether to append the marker, or prepend it.
               """
               from _pytest.mark import MARK_GEN
       
               if isinstance(marker, MarkDecorator):
                   marker_ = marker
               elif isinstance(marker, str):
                   marker_ = getattr(MARK_GEN, marker)
               else:
                   raise ValueError("is not a string or pytest.mark.* Marker")
               self.keywords[marker_.name] = marker_
               if append:
                   self.own_markers.append(marker_.mark)
               else:
                   self.own_markers.insert(0, marker_.mark)
       
    1:     def iter_markers(self, name: str | None = None) -> Iterator[Mark]:
               """Iterate over all markers of the node.
       
               :param name: If given, filter the results by the name attribute.
               :returns: An iterator of the markers of the node.
               """
   96:         return (x[1] for x in self.iter_markers_with_node(name=name))
       
    1:     def iter_markers_with_node(
    1:         self, name: str | None = None
           ) -> Iterator[tuple[Node, Mark]]:
               """Iterate over all markers of the node.
       
               :param name: If given, filter the results by the name attribute.
               :returns: An iterator of (node, mark) tuples.
               """
  378:         for node in self.iter_parents():
  378:             for mark in node.own_markers:
   54:                 if name is None or getattr(mark, "name", None) == name:
                           yield node, mark
       
    2:     @overload
    2:     def get_closest_marker(self, name: str) -> Mark | None: ...
       
    2:     @overload
    2:     def get_closest_marker(self, name: str, default: Mark) -> Mark: ...
       
    1:     def get_closest_marker(self, name: str, default: Mark | None = None) -> Mark | None:
               """Return the first marker matching the name, from closest (for
               example function) to farther level (for example module level).
       
               :param default: Fallback return value if no marker was found.
               :param name: Name to filter by.
               """
               return next(self.iter_markers(name=name), default)
       
    1:     def listextrakeywords(self) -> set[str]:
               """Return a set of all extra keywords in self and any parents."""
               extra_keywords: set[str] = set()
               for item in self.listchain():
                   extra_keywords.update(item.extra_keyword_matches)
               return extra_keywords
       
    1:     def listnames(self) -> list[str]:
               return [x.name for x in self.listchain()]
       
    1:     def addfinalizer(self, fin: Callable[[], object]) -> None:
               """Register a function to be called without arguments when this node is
               finalized.
       
               This method can only be called when this node is active
               in a setup chain, for example during self.setup().
               """
               self.session._setupstate.addfinalizer(fin, self)
       
    1:     def getparent(self, cls: type[_NodeType]) -> _NodeType | None:
               """Get the closest parent node (including self) which is an instance of
               the given class.
       
               :param cls: The node class to search for.
               :returns: The node, if found.
               """
   84:         for node in self.iter_parents():
   72:             if isinstance(node, cls):
    6:                 return node
   12:         return None
       
    1:     def _traceback_filter(self, excinfo: ExceptionInfo[BaseException]) -> Traceback:
               return excinfo.traceback
       
    1:     def _repr_failure_py(
               self,
               excinfo: ExceptionInfo[BaseException],
    1:         style: TracebackStyle | None = None,
           ) -> TerminalRepr:
               from _pytest.fixtures import FixtureLookupError
       
               if isinstance(excinfo.value, ConftestImportFailure):
                   excinfo = ExceptionInfo.from_exception(excinfo.value.cause)
               if isinstance(excinfo.value, fail.Exception):
                   if not excinfo.value.pytrace:
                       style = "value"
               if isinstance(excinfo.value, FixtureLookupError):
                   return excinfo.value.formatrepr()
       
               tbfilter: bool | Callable[[ExceptionInfo[BaseException]], Traceback]
               if self.config.getoption("fulltrace", False):
                   style = "long"
                   tbfilter = False
               else:
                   tbfilter = self._traceback_filter
                   if style == "auto":
                       style = "long"
               # XXX should excinfo.getrepr record all data and toterminal() process it?
               if style is None:
                   if self.config.getoption("tbstyle", "auto") == "short":
                       style = "short"
                   else:
                       style = "long"
       
               if self.config.get_verbosity() > 1:
                   truncate_locals = False
               else:
                   truncate_locals = True
       
               truncate_args = False if self.config.get_verbosity() > 2 else True
       
               # excinfo.getrepr() formats paths relative to the CWD if `abspath` is False.
               # It is possible for a fixture/test to change the CWD while this code runs, which
               # would then result in the user seeing confusing paths in the failure message.
               # To fix this, if the CWD changed, always display the full absolute path.
               # It will be better to just always display paths relative to invocation_dir, but
               # this requires a lot of plumbing (#6428).
               try:
                   abspath = Path(os.getcwd()) != self.config.invocation_params.dir
               except OSError:
                   abspath = True
       
               return excinfo.getrepr(
                   funcargs=True,
                   abspath=abspath,
                   showlocals=self.config.getoption("showlocals", False),
                   style=style,
                   tbfilter=tbfilter,
                   truncate_locals=truncate_locals,
                   truncate_args=truncate_args,
               )
       
    1:     def repr_failure(
               self,
               excinfo: ExceptionInfo[BaseException],
    1:         style: TracebackStyle | None = None,
           ) -> str | TerminalRepr:
               """Return a representation of a collection or test failure.
       
               .. seealso:: :ref:`non-python tests`
       
               :param excinfo: Exception information for the failure.
               """
               return self._repr_failure_py(excinfo, style)
       
       
    1: def get_fslocation_from_item(node: Node) -> tuple[str | Path, int | None]:
           """Try to extract the actual location from a node, depending on available attributes:
       
           * "location": a pair (path, lineno)
           * "obj": a Python object that the node wraps.
           * "path": just a path
       
           :rtype: A tuple of (str|Path, int) with filename and 0-based line number.
           """
           # See Item.location.
           location: tuple[str, int | None, str] | None = getattr(node, "location", None)
           if location is not None:
               return location[:2]
           obj = getattr(node, "obj", None)
           if obj is not None:
               return getfslineno(obj)
           return getattr(node, "path", "unknown location"), -1
       
       
    2: class Collector(Node, abc.ABC):
    1:     """Base class of all collectors.
       
           Collector create children through `collect()` and thus iteratively build
           the collection tree.
           """
       
    2:     class CollectError(Exception):
    1:         """An error during collection, contains a custom message."""
       
    2:     @abc.abstractmethod
    2:     def collect(self) -> Iterable[Item | Collector]:
               """Collect children (items and collectors) for this collector."""
               raise NotImplementedError("abstract")
       
           # TODO: This omits the style= parameter which breaks Liskov Substitution.
    1:     def repr_failure(  # type: ignore[override]
               self, excinfo: ExceptionInfo[BaseException]
           ) -> str | TerminalRepr:
               """Return a representation of a collection failure.
       
               :param excinfo: Exception information for the failure.
               """
               if isinstance(excinfo.value, self.CollectError) and not self.config.getoption(
                   "fulltrace", False
               ):
                   exc = excinfo.value
                   return str(exc.args[0])
       
               # Respect explicit tbstyle option, but default to "short"
               # (_repr_failure_py uses "long" with "fulltrace" option always).
               tbstyle = self.config.getoption("tbstyle", "auto")
               if tbstyle == "auto":
                   tbstyle = "short"
       
               return self._repr_failure_py(excinfo, style=tbstyle)
       
    1:     def _traceback_filter(self, excinfo: ExceptionInfo[BaseException]) -> Traceback:
               if hasattr(self, "path"):
                   traceback = excinfo.traceback
                   ntraceback = traceback.cut(path=self.path)
                   if ntraceback == traceback:
                       ntraceback = ntraceback.cut(excludepath=tracebackcutdir)
                   return ntraceback.filter(excinfo)
               return excinfo.traceback
       
       
    2: @lru_cache(maxsize=1000)
    2: def _check_initialpaths_for_relpath(
           initial_paths: frozenset[Path], path: Path
       ) -> str | None:
           if path in initial_paths:
               return ""
       
           for parent in path.parents:
               if parent in initial_paths:
                   return str(path.relative_to(parent))
       
           return None
       
       
    2: class FSCollector(Collector, abc.ABC):
    1:     """Base class for filesystem collectors."""
       
    1:     def __init__(
               self,
    1:         fspath: None = None,
    1:         path_or_parent: Path | Node | None = None,
    1:         path: Path | None = None,
    1:         name: str | None = None,
    1:         parent: Node | None = None,
    1:         config: Config | None = None,
    1:         session: Session | None = None,
    1:         nodeid: str | None = None,
           ) -> None:
   32:         if path_or_parent:
                   if isinstance(path_or_parent, Node):
                       assert parent is None
                       parent = cast(FSCollector, path_or_parent)
                   elif isinstance(path_or_parent, Path):
                       assert path is None
                       path = path_or_parent
   32:         assert path is not None
       
   32:         if name is None:
   32:             name = path.name
   32:             if parent is not None and parent.path != path:
   31:                 try:
   31:                     rel = path.relative_to(parent.path)
                       except ValueError:
                           pass
                       else:
   31:                     name = str(rel)
   31:                 name = norm_sep(name)
   32:         self.path = path
       
   32:         if session is None:
   31:             assert parent is not None
   31:             session = parent.session
       
   32:         if nodeid is None:
   32:             try:
   32:                 nodeid = str(self.path.relative_to(session.config.rootpath))
                   except ValueError:
                       nodeid = _check_initialpaths_for_relpath(session._initialpaths, path)
       
   32:             if nodeid:
   32:                 nodeid = norm_sep(nodeid)
       
   64:         super().__init__(
   32:             name=name,
   32:             parent=parent,
   32:             config=config,
   32:             session=session,
   32:             nodeid=nodeid,
   32:             path=path,
               )
       
    2:     @classmethod
    2:     def from_parent(
               cls,
               parent,
               *,
    1:         fspath: None = None,
    1:         path: Path | None = None,
               **kw,
           ) -> Self:
               """The public constructor."""
   32:         return super().from_parent(parent=parent, fspath=fspath, path=path, **kw)
       
       
    2: class File(FSCollector, abc.ABC):
    1:     """Base class for collecting tests from a file.
       
           :ref:`non-python tests`.
           """
       
       
    2: class Directory(FSCollector, abc.ABC):
    1:     """Base class for collecting files from a directory.
       
           A basic directory collector does the following: goes over the files and
           sub-directories in the directory and creates collectors for them by calling
           the hooks :hook:`pytest_collect_directory` and :hook:`pytest_collect_file`,
           after checking that they are not ignored using
           :hook:`pytest_ignore_collect`.
       
           The default directory collectors are :class:`~pytest.Dir` and
           :class:`~pytest.Package`.
       
           .. versionadded:: 8.0
       
           :ref:`custom directory collectors`.
           """
       
       
    2: class Item(Node, abc.ABC):
    1:     """Base class of all test invocation items.
       
           Note that for a single function there might be multiple test invocation items.
           """
       
    1:     nextitem = None
       
    1:     def __init__(
               self,
               name,
    1:         parent=None,
    1:         config: Config | None = None,
    1:         session: Session | None = None,
    1:         nodeid: str | None = None,
               **kw,
           ) -> None:
               # The first two arguments are intentionally passed positionally,
               # to keep plugins who define a node type which inherits from
               # (pytest.Item, pytest.File) working (see issue #8435).
               # They can be made kwargs when the deprecation above is done.
   48:         super().__init__(
   12:             name,
   12:             parent,
   12:             config=config,
   12:             session=session,
   12:             nodeid=nodeid,
   12:             **kw,
               )
   12:         self._report_sections: list[tuple[str, str, str]] = []
       
               #: A list of tuples (name, value) that holds user defined properties
               #: for this test.
   12:         self.user_properties: list[tuple[str, object]] = []
       
   12:         self._check_item_and_collector_diamond_inheritance()
       
    1:     def _check_item_and_collector_diamond_inheritance(self) -> None:
               """
               Check if the current type inherits from both File and Collector
               at the same time, emitting a warning accordingly (#8447).
               """
   12:         cls = type(self)
       
               # We inject an attribute in the type to avoid issuing this warning
               # for the same class more than once, which is not helpful.
               # It is a hack, but was deemed acceptable in order to avoid
               # flooding the user in the common case.
   12:         attr_name = "_pytest_diamond_inheritance_warning_shown"
   12:         if getattr(cls, attr_name, False):
   10:             return
    2:         setattr(cls, attr_name, True)
       
   12:         problems = ", ".join(
    5:             base.__name__ for base in cls.__bases__ if issubclass(base, Collector)
               )
    2:         if problems:
                   warnings.warn(
                       f"{cls.__name__} is an Item subclass and should not be a collector, "
                       f"however its bases {problems} are collectors.\n"
                       "Please split the Collectors and the Item into separate node types.\n"
                       "Pytest Doc example: https://docs.pytest.org/en/latest/example/nonpython.html\n"
                       "example pull request on a plugin: https://github.com/asmeurer/pytest-flakes/pull/40/",
                       PytestWarning,
                   )
       
    2:     @abc.abstractmethod
    2:     def runtest(self) -> None:
               """Run the test case for this item.
       
               Must be implemented by subclasses.
       
               .. seealso:: :ref:`non-python tests`
               """
               raise NotImplementedError("runtest must be implemented by Item subclass")
       
    1:     def add_report_section(self, when: str, key: str, content: str) -> None:
               """Add a new report section, similar to what's done internally to add
               stdout and stderr captured output::
       
                   item.add_report_section("call", "stdout", "report section contents")
       
               :param str when:
                   One of the possible capture states, ``"setup"``, ``"call"``, ``"teardown"``.
               :param str key:
                   Name of the section, can be customized at will. Pytest uses ``"stdout"`` and
                   ``"stderr"`` internally.
               :param str content:
                   The full contents as a string.
               """
   54:         if content:
                   self._report_sections.append((when, key, content))
       
    1:     def reportinfo(self) -> tuple[os.PathLike[str] | str, int | None, str]:
               """Get location information for this item for test reports.
       
               Returns a tuple with three elements:
       
               - The path of the test (default ``self.path``)
               - The 0-based line number of the test (default ``None``)
               - A name of the test to be shown (default ``""``)
       
               .. seealso:: :ref:`non-python tests`
               """
               return self.path, None, ""
       
    2:     @cached_property
    2:     def location(self) -> tuple[str, int | None, str]:
               """
               Returns a tuple of ``(relfspath, lineno, testname)`` for this item
               where ``relfspath`` is file path relative to ``config.rootpath``
               and lineno is a 0-based line number.
               """
    6:         location = self.reportinfo()
    6:         path = absolutepath(location[0])
    6:         relfspath = self.session._node_location_to_relpath(path)
    6:         assert type(location[2]) is str
    6:         return (relfspath, location[1], location[2])
