    1: from __future__ import annotations
       
    1: from typing import Any
    1: from typing import cast
    1: from typing import Generic
    1: from typing import TypeVar
       
       
    1: __all__ = ["Stash", "StashKey"]
       
       
    1: T = TypeVar("T")
    1: D = TypeVar("D")
       
       
    2: class StashKey(Generic[T]):
    1:     """``StashKey`` is an object used as a key to a :class:`Stash`.
       
           A ``StashKey`` is associated with the type ``T`` of the value of the key.
       
           A ``StashKey`` is unique and cannot conflict with another key.
       
           .. versionadded:: 7.0
           """
       
    1:     __slots__ = ()
       
       
    2: class Stash:
    1:     r"""``Stash`` is a type-safe heterogeneous mutable mapping that
           allows keys and value types to be defined separately from
           where it (the ``Stash``) is created.
       
           Usually you will be given an object which has a ``Stash``, for example
           :class:`~pytest.Config` or a :class:`~_pytest.nodes.Node`:
       
           .. code-block:: python
       
               stash: Stash = some_object.stash
       
           If a module or plugin wants to store data in this ``Stash``, it creates
           :class:`StashKey`\s for its keys (at the module level):
       
           .. code-block:: python
       
               # At the top-level of the module
               some_str_key = StashKey[str]()
               some_bool_key = StashKey[bool]()
       
           To store information:
       
           .. code-block:: python
       
               # Value type must match the key.
               stash[some_str_key] = "value"
               stash[some_bool_key] = True
       
           To retrieve the information:
       
           .. code-block:: python
       
               # The static type of some_str is str.
               some_str = stash[some_str_key]
               # The static type of some_bool is bool.
               some_bool = stash[some_bool_key]
       
           .. versionadded:: 7.0
           """
       
    1:     __slots__ = ("_storage",)
       
    1:     def __init__(self) -> None:
   46:         self._storage: dict[StashKey[Any], object] = {}
       
    1:     def __setitem__(self, key: StashKey[T], value: T) -> None:
               """Set a value for key."""
   54:         self._storage[key] = value
       
    1:     def __getitem__(self, key: StashKey[T]) -> T:
               """Get the value for key.
       
               Raises ``KeyError`` if the key wasn't set before.
               """
 3867:         return cast(T, self._storage[key])
       
    1:     def get(self, key: StashKey[T], default: D) -> T | D:
               """Get the value for key, or return default if the key wasn't set
               before."""
   36:         try:
   36:             return self[key]
    3:         except KeyError:
    3:             return default
       
    1:     def setdefault(self, key: StashKey[T], default: T) -> T:
               """Return the value of key if already set, otherwise set the value
               of key to default and return default."""
   18:         try:
   18:             return self[key]
    6:         except KeyError:
    6:             self[key] = default
    6:             return default
       
    1:     def __delitem__(self, key: StashKey[T]) -> None:
               """Delete the value for key.
       
               Raises ``KeyError`` if the key wasn't set before.
               """
   15:         del self._storage[key]
       
    1:     def __contains__(self, key: StashKey[T]) -> bool:
               """Return whether key was set."""
    4:         return key in self._storage
       
    1:     def __len__(self) -> int:
               """Return how many items exist in the stash."""
               return len(self._storage)
