    1: """
       Backport Python3.8+ typing utils &amp; issubtype &amp; more
       
       ![Python 3.6](https://github.com/bojiang/typing_utils/workflows/Python%203.6/badge.svg)
       ![Python 3.7](https://github.com/bojiang/typing_utils/workflows/Python%203.7/badge.svg)
       ![Python 3.8](https://github.com/bojiang/typing_utils/workflows/Python%203.8/badge.svg)
       
       ## Install
       
       ``` bash
           pip install typing_utils
       ```
       """
       
    1: import collections.abc
    1: import io
    1: import itertools
    1: import types
    1: import typing
       
    1: if hasattr(typing, "ForwardRef"):  # python3.8
    1:     ForwardRef = getattr(typing, "ForwardRef")
       elif hasattr(typing, "_ForwardRef"):  # python3.6
           ForwardRef = getattr(typing, "_ForwardRef")
       else:
           raise NotImplementedError()
       
    1: if hasattr(typing, "Literal"):
    1:     Literal = getattr(typing, "Literal")
       else:
           Literal = None
       
    1: if hasattr(typing, "_TypedDictMeta"):
    1:     _TypedDictMeta = getattr(typing, "_TypedDictMeta")
       else:
           _TypedDictMeta = None
       
    1: if hasattr(types, "UnionType"):
    1:     UnionType = getattr(types, "UnionType")
       else:
           UnionType = None
       
    1: unknown = None
       
    1: BUILTINS_MAPPING = {
    1:     typing.List: list,
    1:     typing.Set: set,
    1:     typing.Dict: dict,
    1:     typing.Tuple: tuple,
    1:     typing.ByteString: bytes,  # https://docs.python.org/3/library/typing.html#typing.ByteString
    1:     typing.Callable: collections.abc.Callable,
    1:     typing.Sequence: collections.abc.Sequence,
    1:     type(None): None,
       }
       
    1: STATIC_SUBTYPE_MAPPING: typing.Dict[type, typing.Type] = {
    1:     io.TextIOWrapper: typing.TextIO,
    1:     io.TextIOBase: typing.TextIO,
    1:     io.StringIO: typing.TextIO,
    1:     io.BufferedReader: typing.BinaryIO,
    1:     io.BufferedWriter: typing.BinaryIO,
    1:     io.BytesIO: typing.BinaryIO,
       }
       
    1: if UnionType:
       
    1:     def is_union(element: object) -> bool:
 6731:         return element is typing.Union or element is UnionType
       
       else:
       
           def is_union(element: object) -> bool:
               return element is typing.Union
       
       
    1: def optional_all(elements) -> typing.Optional[bool]:
           if all(elements):
               return True
           if all(e is False for e in elements):
               return False
           return unknown
       
       
    1: def optional_any(elements) -> typing.Optional[bool]:
           if any(elements):
               return True
           if any(e is None for e in elements):
               return unknown
           return False
       
       
    1: def _hashable(value):
           """Determine whether `value` can be hashed."""
17768:     try:
17768:         hash(value)
           except TypeError:
               return False
17768:     return True
       
       
    1: get_type_hints = typing.get_type_hints
       
    1: GenericClass = type(typing.List)
    1: UnionClass = type(typing.Union)
       
    1: Type = typing.Union[None, type, "typing.TypeVar"]
    1: OriginType = typing.Union[None, type]
    1: TypeArgs = typing.Union[type, typing.AbstractSet[type], typing.Sequence[type]]
       
       
    1: def _normalize_aliases(type_: Type) -> Type:
17768:     if isinstance(type_, typing.TypeVar):
               return type_
       
17768:     assert _hashable(type_), "_normalize_aliases should only be called on element types"
       
17768:     if type_ in BUILTINS_MAPPING:
  640:         return BUILTINS_MAPPING[type_]  # type: ignore
17128:     return type_
       
       
    1: def get_origin(type_):
           """Get the unsubscripted version of a type.
           This supports generic types, Callable, Tuple, Union, Literal, Final and ClassVar.
           Return None for unsupported types.
       
           Examples:
       
           ```python
               from typing_utils import get_origin
       
               get_origin(Literal[42]) is Literal
               get_origin(int) is None
               get_origin(ClassVar[int]) is ClassVar
               get_origin(Generic) is Generic
               get_origin(Generic[T]) is Generic
               get_origin(Union[T, int]) is Union
               get_origin(List[Tuple[T, T]][int]) == list
           ```
           """
 8884:     if hasattr(typing, "get_origin"):  # python 3.8+
 8884:         _getter = getattr(typing, "get_origin")
 8884:         ori = _getter(type_)
           elif hasattr(typing.List, "_special"):  # python 3.7
               if isinstance(type_, GenericClass) and not type_._special:
                   ori = type_.__origin__
               elif hasattr(type_, "_special") and type_._special:
                   ori = type_
               elif type_ is typing.Generic:
                   ori = typing.Generic
               else:
                   ori = None
           else:  # python 3.6
               if isinstance(type_, GenericClass):
                   ori = type_.__origin__
                   if ori is None:
                       ori = type_
               elif isinstance(type_, UnionClass):
                   ori = type_.__origin__
               elif type_ is typing.Generic:
                   ori = typing.Generic
               else:
                   ori = None
 8884:     if ori is None and _TypedDictMeta and isinstance(type_, _TypedDictMeta):
   32:         ori = dict
 8884:     return _normalize_aliases(ori)
       
       
    1: def get_args(type_) -> typing.Tuple:
           """Get type arguments with all substitutions performed.
           For unions, basic simplifications used by Union constructor are performed.
       
           Examples:
       
           ```python
               from typing_utils import get_args
       
               get_args(Dict[str, int]) == (str, int)
               get_args(int) == ()
               get_args(Union[int, Union[T, int], str][int]) == (int, str)
               get_args(Union[int, Tuple[T, int]][str]) == (int, Tuple[str, int])
               get_args(Callable[[], T][int]) == ([], int)
           ```
           """
19561:     if hasattr(typing, "get_args"):  # python 3.8+
19561:         _getter = getattr(typing, "get_args")
19561:         res = _getter(type_)
           elif hasattr(typing.List, "_special"):  # python 3.7
               if (
                   isinstance(type_, GenericClass) and not type_._special  # type: ignore
               ):  # backport for python 3.8
                   res = type_.__args__  # type: ignore
                   if get_origin(type_) is collections.abc.Callable and res[0] is not Ellipsis:
                       res = (list(res[:-1]), res[-1])
               else:
                   res = ()
           else:  # python 3.6
               if isinstance(type_, (GenericClass, UnionClass)):  # backport for python 3.8
                   res = type_.__args__  # type: ignore
                   if get_origin(type_) is collections.abc.Callable and res[0] is not Ellipsis:
                       res = (list(res[:-1]), res[-1])
               else:
                   res = ()
19561:     if _TypedDictMeta and isinstance(type_, _TypedDictMeta):
   64:         return str, typing.Any
19497:     return () if res is None else res
       
       
    1: def eval_forward_ref(ref, forward_refs=None):
           """
           eval forward_refs in all cPython versions
           """
           localns = forward_refs or {}
       
           if hasattr(typing, "_eval_type"):  # python3.8 & python 3.9
               _eval_type = getattr(typing, "_eval_type")
               return _eval_type(ref, globals(), localns)
       
           if hasattr(ref, "_eval_type"):  # python3.6
               _eval_type = getattr(ref, "_eval_type")
               return _eval_type(globals(), localns)
       
           raise NotImplementedError()
       
       
    2: class NormalizedType(typing.NamedTuple):
    1:     """
           Normalized type, made it possible to compare, hash between types.
           """
       
    1:     origin: Type
    1:     args: typing.Union[tuple, frozenset] = tuple()
       
    1:     def __eq__(self, other):
11772:         if isinstance(other, NormalizedType):
11772:             if self.origin != other.origin:
                       return False
11772:             if isinstance(self.args, frozenset) and isinstance(other.args, frozenset):
 1136:                 return self.args <= other.args and other.args <= self.args
10636:             return self.origin == other.origin and self.args == other.args
               if not self.args:
                   return self.origin == other
               return False
       
    1:     def __hash__(self) -> int:
 7352:         if not self.args:
 3392:             return hash(self.origin)
 3960:         return hash((self.origin, self.args))
       
    1:     def __repr__(self):
               if not self.args:
                   return f"{self.origin}"
               return f"{self.origin}[{self.args}])"
       
       
    1: def _normalize_args(tps: TypeArgs):
14256:     if isinstance(tps, str):
 1792:         return tps
12464:     if isinstance(tps, collections.abc.Sequence):
10416:         return tuple(_normalize_args(type_) for type_ in tps)
 9264:     if isinstance(tps, collections.abc.Set):
 8640:         return frozenset(_normalize_args(type_) for type_ in tps)
 7664:     return normalize(tps)
       
       
    1: def normalize(type_: Type) -> NormalizedType:
           """
           convert types to NormalizedType instances.
           """
 8884:     args = get_args(type_)
 8884:     origin = get_origin(type_)
 8884:     if not origin:
 4084:         return NormalizedType(_normalize_aliases(type_))
 4800:     origin = _normalize_aliases(origin)
       
 4800:     if is_union(origin):  # sort args when the origin is Union
 1600:         args = _normalize_args(frozenset(args))
           else:
 3200:         args = _normalize_args(args)
 4800:     return NormalizedType(origin, args)
       
       
    1: def _is_origin_subtype(left: OriginType, right: OriginType) -> bool:
  441:     if left is right:
  441:         return True
       
           if (
               left is not None
               and left in STATIC_SUBTYPE_MAPPING
               and right == STATIC_SUBTYPE_MAPPING[left]
           ):
               return True
       
           if hasattr(left, "mro"):
               for parent in left.mro():  # type: ignore
                   if parent == right:
                       return True
       
           if isinstance(left, type) and isinstance(right, type):
               return issubclass(left, right)
       
           return left == right
       
       
    2: NormalizedTypeArgs = typing.Union[
    2:     typing.Tuple[typing.Any, ...],
    1:     typing.FrozenSet[NormalizedType],
    1:     NormalizedType,
       ]
       
       
    5: def _is_origin_subtype_args(
    1:     left: "NormalizedTypeArgs",
    1:     right: "NormalizedTypeArgs",
    1:     forward_refs: typing.Optional[typing.Mapping[str, type]],
    1: ) -> typing.Optional[bool]:
  560:     if isinstance(left, frozenset):
  280:         if not isinstance(right, frozenset):
                   return False
       
  280:         excluded = left - right
  280:         if not excluded:
                   # Union[str, int] <> Union[int, str]
  280:             return True
       
               # Union[list, int] <> Union[typing.Sequence, int]
               return all(
                   any(_is_normal_subtype(e, r, forward_refs) for r in right) for e in excluded
               )
       
  560:     if isinstance(left, collections.abc.Sequence) and not isinstance(
  280:         left, NormalizedType
           ):
  248:         if not isinstance(right, collections.abc.Sequence) or isinstance(
  124:             right, NormalizedType
               ):
                   return False
       
  124:         if (
  124:             left
  248:             and left[-1].origin is not Ellipsis
  124:             and right
  124:             and right[-1].origin is Ellipsis
               ):
                   # Tuple[type, type] <> Tuple[type, ...]
                   return all(_is_origin_subtype_args(l, right[0], forward_refs) for l in left)
       
  124:         if len(left) != len(right):
                   return False
       
  684:         return all(
  312:             l is not None
  156:             and r is not None
  156:             and _is_origin_subtype_args(l, r, forward_refs)
  280:             for l, r in itertools.zip_longest(left, right)
               )
       
  156:     assert isinstance(left, NormalizedType)
  156:     assert isinstance(right, NormalizedType)
       
  156:     return _is_normal_subtype(left, right, forward_refs)
       
       
    5: def _is_normal_subtype(
    1:     left: NormalizedType,
    1:     right: NormalizedType,
    1:     forward_refs: typing.Optional[typing.Mapping[str, type]],
    1: ) -> typing.Optional[bool]:
  766:     if isinstance(left.origin, ForwardRef):
               left = normalize(eval_forward_ref(left.origin, forward_refs=forward_refs))
       
  766:     if isinstance(right.origin, ForwardRef):
               right = normalize(eval_forward_ref(right.origin, forward_refs=forward_refs))
       
           # Any
  766:     if right.origin is typing.Any:
   29:         return True
       
           # Union
  737:     if is_union(right.origin) and is_union(left.origin):
  280:         return _is_origin_subtype_args(left.args, right.args, forward_refs)
  457:     if is_union(right.origin):
               return optional_any(
                   _is_normal_subtype(left, a, forward_refs) for a in right.args
               )
  457:     if is_union(left.origin):
               return optional_all(
                   _is_normal_subtype(a, right, forward_refs) for a in left.args
               )
       
           # Literal
  457:     if right.origin is Literal:
   16:         if left.origin is not Literal:
                   return False
   16:         return set(left.args).issubset(set(right.args))
       
           # TypeVar
  441:     if isinstance(left.origin, typing.TypeVar) and isinstance(
               right.origin, typing.TypeVar
           ):
               if left.origin is right.origin:
                   return True
       
               left_bound = getattr(left.origin, "__bound__", None)
               right_bound = getattr(right.origin, "__bound__", None)
               if right_bound is None or left_bound is None:
                   return unknown
               return _is_normal_subtype(
                   normalize(left_bound), normalize(right_bound), forward_refs
               )
  441:     if isinstance(right.origin, typing.TypeVar):
               return unknown
  441:     if isinstance(left.origin, typing.TypeVar):
               left_bound = getattr(left.origin, "__bound__", None)
               if left_bound is None:
                   return unknown
               return _is_normal_subtype(normalize(left_bound), right, forward_refs)
       
  441:     if not left.args and not right.args:
  317:         return _is_origin_subtype(left.origin, right.origin)
       
  124:     if not right.args:
               return _is_origin_subtype(left.origin, right.origin)
       
  124:     if _is_origin_subtype(left.origin, right.origin):
  124:         return _is_origin_subtype_args(left.args, right.args, forward_refs)
       
           return False
       
       
    5: def issubtype(
    1:     left: Type,
    1:     right: Type,
    2:     forward_refs: typing.Optional[dict] = None,
    1: ) -> typing.Optional[bool]:
           """Check that the left argument is a subtype of the right.
           For unions, check if the type arguments of the left is a subset of the right.
           Also works for nested types including ForwardRefs.
       
           Examples:
       
           ```python
               from typing_utils import issubtype
       
               issubtype(typing.List, typing.Any) == True
               issubtype(list, list) == True
               issubtype(list, typing.List) == True
               issubtype(list, typing.Sequence) == True
               issubtype(typing.List[int], list) == True
               issubtype(typing.List[typing.List], list) == True
               issubtype(list, typing.List[int]) == False
               issubtype(list, typing.Union[typing.Tuple, typing.Set]) == False
               issubtype(typing.List[typing.List], typing.List[typing.Sequence]) == True
               JSON = typing.Union[
                   int, float, bool, str, None, typing.Sequence["JSON"],
                   typing.Mapping[str, "JSON"]
               ]
               issubtype(str, JSON, forward_refs={'JSON': JSON}) == True
               issubtype(typing.Dict[str, str], JSON, forward_refs={'JSON': JSON}) == True
               issubtype(typing.Dict[str, bytes], JSON, forward_refs={'JSON': JSON}) == False
           ```
           """
  610:     return _is_normal_subtype(normalize(left), normalize(right), forward_refs)
       
       
    1: __all__ = [
           "issubtype",
           "get_origin",
           "get_args",
           "get_type_hints",
       ]
