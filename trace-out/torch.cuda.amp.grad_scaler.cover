    1: from typing_extensions import deprecated
       
    1: import torch
       
       # We need to keep this unused import for BC reasons
    1: from torch.amp.grad_scaler import OptState  # noqa: F401
       
       
    1: __all__ = ["GradScaler"]
       
       
    2: class GradScaler(torch.amp.GradScaler):
    1:     r"""
           See :class:`torch.amp.GradScaler`.
           ``torch.cuda.amp.GradScaler(args...)`` is deprecated. Please use ``torch.amp.GradScaler("cuda", args...)`` instead.
           """
       
    3:     @deprecated(
    1:         "`torch.cuda.amp.GradScaler(args...)` is deprecated. "
               "Please use `torch.amp.GradScaler('cuda', args...)` instead.",
    1:         category=FutureWarning,
           )
    8:     def __init__(
               self,
    2:         init_scale: float = 2.0**16,
    2:         growth_factor: float = 2.0,
    2:         backoff_factor: float = 0.5,
    2:         growth_interval: int = 2000,
    2:         enabled: bool = True,
    1:     ) -> None:
               super().__init__(
                   "cuda",
                   init_scale=init_scale,
                   growth_factor=growth_factor,
                   backoff_factor=backoff_factor,
                   growth_interval=growth_interval,
                   enabled=enabled,
               )
