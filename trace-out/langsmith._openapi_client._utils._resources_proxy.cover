    1: from __future__ import annotations
       
    1: from typing import Any
    1: from typing_extensions import override
       
    1: from ._proxy import LazyProxy
       
       
    2: class ResourcesProxy(LazyProxy[Any]):
    1:     """A proxy for the `langsmith_api.resources` module.
       
           This is used so that we can lazily import `langsmith_api.resources` only when
           needed *and* so that users can just import `langsmith_api` and reference `langsmith_api.resources`
           """
       
    2:     @override
    2:     def __load__(self) -> Any:
               import importlib
       
               mod = importlib.import_module("langsmith_api.resources")
               return mod
       
       
    1: resources = ResourcesProxy().__as_proxied__()
