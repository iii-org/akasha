    1: """Document loaders."""
       
    1: from typing import TYPE_CHECKING
       
    1: from langchain_core._import_utils import import_attr
       
    1: if TYPE_CHECKING:
           from langchain_core.document_loaders.base import BaseBlobParser, BaseLoader
           from langchain_core.document_loaders.blob_loaders import Blob, BlobLoader, PathLike
           from langchain_core.document_loaders.langsmith import LangSmithLoader
       
    1: __all__ = (
           "BaseBlobParser",
           "BaseLoader",
           "Blob",
           "BlobLoader",
           "LangSmithLoader",
           "PathLike",
       )
       
    1: _dynamic_imports = {
    1:     "BaseBlobParser": "base",
    1:     "BaseLoader": "base",
    1:     "Blob": "blob_loaders",
    1:     "BlobLoader": "blob_loaders",
    1:     "PathLike": "blob_loaders",
    1:     "LangSmithLoader": "langsmith",
       }
       
       
    1: def __getattr__(attr_name: str) -> object:
    4:     module_name = _dynamic_imports.get(attr_name)
    4:     result = import_attr(attr_name, module_name, __spec__.parent)
    4:     globals()[attr_name] = result
    4:     return result
       
       
    1: def __dir__() -> list[str]:
           return list(__all__)
