pytest:
  image: ghcr.io/astral-sh/uv:0.6.13-python3.10-bookworm-slim
  stage: jobs
  cache:
    key: uv-cache
    paths:
      - /root/.cache/uv
  before_script:
    - python -V
    - uv pip install --system -e ".[dev]" 
  script:
    - pytest
  only:
    - ceres
